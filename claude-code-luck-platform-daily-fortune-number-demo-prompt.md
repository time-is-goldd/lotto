# Claude Code 실행 프롬프트 — 일일 운세 제한·생년월일 UX·연령정책·번호 생성·공개 데모

현재 Luck Platform 저장소에서 아래 제품 정책과 UX를 실제로 구현하고 검증해줘. 단순히 조언하거나 코드 예시만 제시하지 말고, 기존 코드와 미커밋 변경사항을 먼저 감사한 뒤 수정·테스트·최종 보고까지 완료해라.

## 0. 작업 전 원칙

- 현재 공개 기준 URL: `https://lotto-blue-sigma.vercel.app`
- 공식 브랜드: `Luck Platform`
- 기존 OAuth `next/reason`, 꿈 숫자 연동, 운세 deterministic logic, analytics abstraction, 개인정보처리방침, 모바일 BottomNav 작업을 보존한다.
- `git status`와 전체 diff를 먼저 확인하고 사용자 변경을 reset, checkout, stash, discard하지 않는다.
- 기존 구조와 테스트를 확인한 뒤 중복 기능을 만들지 말고 현재 아키텍처에 맞춰 수정한다.
- 새로운 무거운 dependency, 기기 fingerprinting, IP 기반 차단, 인증 우회 기능을 추가하지 않는다.
- 작업 완료 후에도 commit/push하지 말고 diff 요약과 권장 commit message를 제시해 승인을 기다린다.

---

# 1. 제품 정책 결정

이번 작업에서 `하루 1회`는 다음 의미로 구현한다.

> 하루에 새로운 오늘의 운세를 한 번만 생성하고, 같은 날 다시 방문하면 이미 생성된 같은 결과를 제한 없이 다시 확인한다.

결과를 딱 한 번만 보여주고 다시는 못 보게 하는 방식은 사용하지 않는다. 사용자가 실수로 창을 닫거나 결과를 충분히 읽지 못했을 때 불필요한 불만을 만들기 때문이다.

기준 날짜는 브라우저가 실행된 후 24시간이 아니라 `Asia/Seoul` 기준 달력 날짜다.

예:

- 2026-08-19 23:50에 생성 → 그날에는 같은 결과 재확인
- 2026-08-20 00:00 이후 → 새 운세 생성 가능

---

# P0. 오늘의 운세 하루 1회 생성

## 2. 로그인 회원 정책

- 회원은 `user_id + fortune_date(KST)` 기준으로 하루에 운세 결과 한 건만 가진다.
- 이미 오늘 결과가 있으면 새로 계산하거나 새 row를 만들지 않고 기존 결과를 반환한다.
- DB에서 가능한 경우 `user_id, fortune_date` unique constraint 또는 동등한 강제 장치를 사용한다.
- 동시에 여러 요청이 들어와도 중복 row가 생기지 않게 idempotent upsert/transaction을 사용한다.
- 같은 날 `오늘의 맞춤 운세 보기` 또는 `오늘의 운세 다시 보기`를 눌러도 결과 내용은 절대 바뀌지 않는다.
- 다음 한국 날짜가 되면 새 결과를 한 번 생성할 수 있다.
- 프로필 정보를 수정해도 이미 생성된 오늘 결과는 바꾸지 않는다. 변경된 정보는 다음 날부터 반영하고 이 규칙을 화면에서 알린다.

## 3. 비회원 정책과 기술적 한계

비회원의 `컴퓨터당 1회`를 서버가 완벽하게 식별할 수 있다고 표현하지 마라. 로그인하지 않은 사용자는 시크릿 모드, 다른 브라우저, 저장소 삭제, 다른 기기를 통해 제한을 우회할 수 있다.

이번 구현 범위에서는 다음의 `soft limit`을 사용한다.

- 브라우저 localStorage를 사용한다.
- IP 주소 또는 광고성 device fingerprint를 사용하지 않는다.
- 비회원은 `동일 브라우저 + 동일 입력 프로필 + 동일 KST 날짜` 기준으로 새 운세를 한 번만 생성한다.
- 같은 브라우저에서 부모가 본인과 자녀 등 서로 다른 생년월일을 입력할 수 있으므로, 기기 전체 한 건이 아니라 입력 프로필별 한 건으로 제한한다.
- 같은 프로필로 다시 확인하면 새로운 결과가 아니라 저장된 같은 날 결과를 보여준다.
- 사용자가 storage를 삭제하거나 다른 브라우저를 사용하면 다시 생성할 수 있다는 한계를 문서에 기록한다. 이를 막기 위한 invasive tracking은 하지 않는다.

## 4. 비회원 저장 구조

비회원 결과를 다시 보여주려면 브라우저 저장이 필요하므로, 이전의 `아무것도 저장하지 않는다`는 정책과 문구를 실제 동작에 맞게 수정해라.

- 서버 DB에는 비회원 생년월일, 성별, 태어난 시각, 운세 결과를 저장하지 않는다.
- URL query, server log, analytics payload에 raw 입력값을 넣지 않는다.
- localStorage에는 version, KST date key, 입력 프로필을 구분하기 위한 로컬 키, 생성된 결과, 생성 시각만 저장한다.
- raw 생년월일을 key 이름이나 저장 value에 그대로 넣지 말고 versioned local profile key를 사용한다.
- 브라우저 최초 사용 시 `crypto.getRandomValues()`로 device salt를 하나 만들고, Web Crypto SHA-256으로 `schemaVersion + deviceSalt + normalizedBirthDate + genderOrUnknown + birthTimeOrUnknown`을 조합한 profile key를 만든다.
- device salt와 profile key는 브라우저 밖으로 전송하지 않는다. 이는 비밀번호 보안 기능이 아니라 로컬 저장소에 생년월일을 평문으로 노출하지 않기 위한 최소 조치임을 코드 주석에 명확히 한다.
- localStorage 값은 사용자의 브라우저 안에서만 사용하며 서버로 전송하지 않는다.
- schema version을 두고 깨진 값·오래된 값·다른 버전은 안전하게 폐기한다.
- localStorage 접근이 차단되거나 quota 오류가 나면 사이트가 깨지지 않게 in-memory fallback과 안내를 제공한다.
- 전날 결과는 필요하면 최근 7일 정도까지만 로컬에 유지하거나 오늘 결과만 유지한다. 무기한 누적하지 않는다.

개인정보 안내 권장 문구:

> 비회원 운세 정보와 결과는 이 브라우저에만 임시 저장되며 서버 계정에는 저장되지 않습니다. 브라우저 데이터를 삭제하면 기록도 사라질 수 있어요.

실제 구현과 다르면 문구를 조정하고 거짓으로 `저장하지 않는다`고 쓰지 마라.

## 5. 비회원 재방문 UX

같은 날 같은 프로필로 다시 제출했을 때:

- `오늘의 운세는 이미 정해졌어요`라고 안내한다.
- CTA는 `오늘의 운세 다시 보기`로 표시한다.
- 짧은 reveal animation 뒤 저장된 동일 결과를 보여준다.
- 새 결과를 계산하거나 analytics의 신규 생성 이벤트를 다시 보내지 않는다.
- 입력 전에 오늘 저장된 결과가 하나 있으면 `오늘 확인한 운세 다시 보기` 바로가기를 제공할 수 있다. 여러 프로필 결과가 있으면 생년월일을 화면에 나열하지 말고 확인 시각 기준의 비식별 목록 또는 생년월일 재입력 방식으로 구분한다.

다음 날:

- 이전 결과를 그대로 재사용하지 않는다.
- `오늘의 맞춤 운세 보기` 버튼으로 새 결과 한 건을 만든다.

## 6. 운세 이벤트를 생성과 열람으로 분리

기존 analytics abstraction을 재사용해 다음을 구분한다.

| 이벤트 | 의미 |
|---|---|
| `fortune_generation_started` | 오늘 새 운세 생성 요청 |
| `fortune_generated` | 오늘 새 결과가 실제 생성됨 |
| `fortune_result_reopened` | 같은 날 기존 결과 재확인 |
| `fortune_limit_hit` | 같은 날 새 생성 대신 기존 결과로 전환 |

- 결과를 다시 보는 행위를 신규 생성으로 집계하지 않는다.
- 생년월일, 성별 값, 태어난 시각, 운세 원문, profile key를 analytics로 보내지 않는다.
- `auth_state`, `has_gender`, `has_birth_time`, `is_repeat_view` 같은 비식별 boolean만 허용한다.

---

# P0. 생년월일 입력 UX 개선

## 7. 권장 입력 방식

수십 년을 넘겨야 하는 달력 picker 하나에만 의존하지 않는다. 기본 UI는 한국 사용자가 빠르게 입력할 수 있는 분할형 입력을 사용한다.

구조:

- `YYYY` 4자리 연도 입력
- `MM` 2자리 월 입력
- `DD` 2자리 일 입력
- 각 필드 옆에 `년`, `월`, `일` 표시

예시:

> 1999 년 / 01 월 / 10 일

구현 조건:

- 모바일에서 숫자 키패드가 열리도록 `inputMode="numeric"`를 사용한다.
- `autocomplete="bday"` 또는 연·월·일에 맞는 autocomplete 속성을 가능한 범위에서 적용한다.
- 연도 4자리 입력 후 월, 월 2자리 입력 후 일로 자연스럽게 이동한다.
- Backspace 시 앞 필드로 자연스럽게 돌아갈 수 있게 한다.
- `19990110`, `1999-01-10`, `1999.01.10`을 붙여넣으면 세 필드에 올바르게 분배한다.
- 월/일 한 자리도 입력할 수 있고 blur 또는 제출 시 정규화한다.
- 해당 월의 실제 일수와 윤년을 검증한다.
- 존재하지 않는 날짜, 미래 날짜, 지나치게 비현실적인 날짜를 명확히 안내한다.
- 연령 제한을 위한 검증은 하지 않는다.
- 필드마다 label과 오류 연결을 제공하고 placeholder만 label로 사용하지 않는다.
- 모바일 320px에서도 한 줄 또는 읽기 쉬운 두 줄로 배치되게 한다.
- 기존 디자인 시스템과 form component를 재사용한다.

## 8. 선택 입력

- 성별은 `남성`, `여성`, `선택 안 함`을 제공하고 강제하지 않는다.
- 태어난 시각은 선택 항목으로 유지한다.
- 태어난 시각 옆에 `몰라도 괜찮아요`라고 표시한다.
- 선택한 성별·태어난 시각이 실제 운세 계산에 반영되는지 기존 domain logic을 테스트한다.
- 사용하지 않는 정보를 수집하지 않는다.

## 9. 접근성과 오류 처리

- 전체 그룹에 `생년월일` label 또는 fieldset/legend를 제공한다.
- 오류가 나면 어느 값이 잘못됐는지 구체적으로 알린다.
- 예: `2월 30일은 존재하지 않는 날짜예요.`
- 제출 시 첫 오류 필드로 focus를 이동한다.
- screen reader가 연도·월·일을 각각 구분할 수 있어야 한다.
- 키보드만으로 모든 입력과 제출이 가능해야 한다.

---

# P0. 19세 제한 정책 수정

## 10. 공개 운세에서 19세 제한 제거

오늘의 운세는 복권 구매가 아니므로 `/fortune` 공개 열람에는 만 19세 제한을 적용하지 않는다.

- 비회원 운세 폼에서 `isAtLeast19`, 미성년 차단, 19세 경고 오류를 제거한다.
- 자녀 생년월일과 19세 미만 이용자의 생년월일도 유효한 과거 날짜라면 운세를 생성할 수 있게 한다.
- `만 19세 이상만 이용 가능`이라는 문구가 운세 페이지·About·FAQ에 있다면 제거하거나 범위를 정확하게 수정한다.
- 날짜 입력의 목적은 개인화이며 성인 여부 확인이 아니라고 설명한다.

## 11. 복권 구매 제한과 혼동하지 않기

19세 제한을 서비스 전체에서 무작정 삭제하지 마라.

- 대한민국에서는 19세 미만이 복권을 구매할 수 있다는 식의 문구를 절대 사용하지 않는다.
- 번호 생성 페이지에는 아래 취지의 고지를 유지한다.

> 번호는 오락을 위한 조합이며 복권 구매 기능이 아닙니다. 19세 미만은 법에 따라 복권을 구매할 수 없습니다.

- 서비스가 복권을 판매·중개·대리구매하는 것처럼 표현하지 않는다.
- 이용약관에서는 `운세·꿈해몽 공개 콘텐츠 이용`과 `복권 구매 제한`을 별도 항목으로 구분한다.

## 12. 만 14세 미만 계정은 이번 범위에서 열지 않기

공개 운세 열람 제한 해제와 미성년자 회원가입 허용은 별개의 문제다.

- 이번 작업에서는 만 14세 미만의 카카오 회원가입 및 개인정보 DB 저장을 새로 허용하지 않는다.
- 법정대리인 동의·확인 절차 없이 만 14세 미만의 생년월일·성별·기록을 회원 계정에 저장하지 않는다.
- 자녀 운세는 우선 비회원 브라우저 내 계산으로 제공한다.
- 기존 계정 가입 기준이 만 19세라면 이번 작업에서 조용히 전체 해제하지 말고, 공개 운세만 연령 제한 없이 이용되도록 분리한다.
- 향후 14~18세 계정을 허용할지 여부는 별도 정책 결정과 약관·개인정보 검토 대상으로 남긴다.

## 13. 비회원 미성년 데이터의 서버 전송 최소화

- 가능하면 비회원 운세 계산은 기존 순수 deterministic function을 브라우저에서 실행해 raw 생년월일이 서버로 전송되지 않게 한다.
- 알고리즘에 secret이 필요하지 않다면 server action을 억지로 거치지 않는다.
- 회원 운세는 기존 인증·DB 경로를 유지한다.
- 비회원과 회원이 같은 입력·같은 KST 날짜라면 기본 결과가 일관되는지 공통 fixture로 테스트한다.

정책 문구를 수정할 때 참고할 공식 근거:

- 동행복권 로또6/45 구매 안내: `https://www.dhlottery.co.kr/lt645/intro`
- 개인정보 보호법 제22조의2 아동의 개인정보 보호: `https://www.law.go.kr/LSW//lsLinkCommonInfo.do?ancYnChk=&chrClsCd=010202&lsJoLnkSeq=1029334873`

---

# P0. 번호 생성 시작 방식 변경

## 14. 제품 결정

단순히 `/generate` 탭에 들어왔다는 이유만으로 자동 생성하지 않는다. 페이지 조회 수를 번호 생성 수로 부풀리는 것은 의미 있는 데이터가 아니다.

다음의 혼합 방식을 사용한다.

### 직접 진입 또는 BottomNav `번호생성` 탭

- 초기 상태에서는 번호를 만들지 않는다.
- 6개 빈 공 또는 `?` placeholder와 설명을 보여준다.
- Primary CTA `행운 번호 생성하기`를 눌러야 생성한다.
- 로그인 회원도 버튼 클릭 전에는 DB 저장 row를 만들지 않는다.

### 꿈 상세 페이지의 명시적 CTA

- 사용자가 이미 `이 꿈의 숫자로 번호 완성하기`를 눌렀다면 그 클릭이 생성 의사 표현이다.
- 이 경로에서는 `/generate?dream=...` 진입 후 기존처럼 자동 reveal을 시작해도 된다.
- 꿈 숫자는 유지하고 나머지만 무작위로 채우는 기존 로직을 보존한다.
- 일반 탭 진입과 꿈 CTA 진입을 `source`로 명확히 구분한다.

### 다시 생성

- 결과가 나온 뒤 `다시 생성하기`를 눌러야 새 조합을 만든다.
- 꿈 source에서는 꿈 숫자는 고정하고 무작위 부분만 바꾼다.
- 일반 source에서는 6개 전체를 새로 만든다.

## 15. 직접 진입 화면 문구

H1:

> 행운 번호 만들기

설명:

> 버튼을 누르면 1부터 45까지 중복 없는 숫자 6개를 만들어드려요.

CTA:

> 행운 번호 생성하기

고지:

> 번호는 재미를 위한 무작위 조합이며 당첨을 보장하거나 확률을 높이지 않습니다.

## 16. 분석 데이터 정리

- 페이지 진입은 `generate_page_viewed`로 측정한다.
- 사용자가 버튼을 눌러 실제 6개가 완성된 뒤에만 `numbers_generated`를 보낸다.
- 자동 page mount를 생성 이벤트로 기록하지 않는다.
- 비즈니스 KPI는 생성 횟수 자체보다 `생성 → 저장 CTA → 실제 저장 → 결과 비교` 전환율을 우선한다.

---

# P1. 로그인 후 화면 검토용 공개 데모

## 17. 권장 방식

카카오 로그인을 우회하거나 GPT에게 실제 계정·쿠키·개인정보를 제공하지 않는다. 별도의 `public demo route`를 만든다.

권장 경로:

- `/demo`
- `/demo/fortune`
- `/demo/journal`
- `/demo/results`

목적은 인증 이후의 UX를 외부 리뷰어가 안전하게 보는 것이다.

## 18. 데모 보안 원칙

- 실제 Supabase 사용자 데이터에 접근하지 않는다.
- Kakao OAuth를 우회하지 않는다.
- 실제 `/my/*` route의 auth guard에 예외 조건을 추가하지 않는다.
- URL query, 쿠키, localStorage 값으로 production auth를 가짜로 통과시키지 않는다.
- 실제 관리자 또는 사용자 계정을 만들고 credentials를 공유하지 않는다.
- 모든 데이터는 코드에 정의된 명백한 synthetic fixture다.
- 실명, 실제 생년월일, 이메일, 실제 번호 기록 등 개인정보를 넣지 않는다.
- 삭제·저장·회원탈퇴·알림 전송 같은 실제 mutation API를 호출하지 않는다.
- 데모의 버튼은 화면 상태만 바꾸거나 비활성화하고 DB에 쓰지 않는다.
- demo route에서 Supabase write/network call이 발생하지 않는 테스트를 추가한다.

## 19. 컴포넌트 구조

인증 페이지를 통째로 복사해 별도 유지보수하지 않는다.

- `presentational component`와 `data/auth adapter`를 분리한다.
- 실제 페이지는 인증·Supabase 데이터 adapter를 사용한다.
- 데모 페이지는 fixture adapter를 사용한다.
- 같은 DiaryList, FortuneCard, ResultComparison 같은 UI 컴포넌트를 재사용한다.
- 실제 화면을 수정하면 데모도 자연스럽게 함께 바뀌게 구성한다.

## 20. 데모에 포함할 상태

### `/demo/fortune`

- 오늘 운세 보기 전 `ready`
- 공개 중 `revealing`
- 결과가 보이는 `result`
- 같은 날 다시 보는 `repeat result`

### `/demo/journal`

- 빈 기록 상태
- 생성 번호, 꿈 기록, 오늘의 운세가 있는 populated 상태
- 번호 저장 CTA와 상세 카드

### `/demo/results`

- 추첨 전 대기 상태
- 저장 번호와 결과 비교 상태
- 일치 없음과 일부 숫자 일치 상태
- 데이터 출처·기준 시각 표시

각 페이지 상단에 다음 배너를 고정한다.

> 데모 화면 · 실제 사용자 정보가 아닌 검토용 예시 데이터입니다.

## 21. 리뷰 편의 기능

- `/demo`에서 각 페이지와 상태로 바로 이동할 수 있게 한다.
- query 또는 화면의 상태 전환 control로 `empty`, `ready`, `revealing`, `result`, `error`를 재현할 수 있게 한다.
- 상태 query는 demo route에서만 허용하고 실제 페이지 로직에 영향을 주지 않는다.
- 모바일과 데스크톱 모두 검토 가능해야 한다.
- reviewer가 무엇을 확인해야 하는지 짧은 설명을 제공한다.

## 22. 검색 노출 차단

- 모든 `/demo/*` route에 `noindex, nofollow`를 적용한다.
- sitemap에서 제외한다.
- robots에서 `/demo/`를 disallow한다.
- Header·Footer·BottomNav 등 일반 사용자 탐색에는 노출하지 않는다.
- analytics에서 제외하거나 `is_demo`로 완전히 필터링해 실제 KPI를 오염시키지 않는다.
- 가능하면 `ENABLE_PUBLIC_DEMO` 환경변수로 켜고 끌 수 있게 한다. 꺼졌을 때는 404를 반환한다.

---

# 문구와 정책 페이지 동기화

## 23. 수정해야 할 설명

기능 변경 후 Home, About, FAQ, Terms, Privacy가 실제 동작과 일치하는지 전수 확인한다.

반영할 내용:

- 오늘의 운세는 로그인 없이 이용 가능
- 새 운세는 프로필별 하루 한 번 생성
- 같은 날에는 동일 결과 재확인 가능
- 회원은 저장된 정보로 반복 입력 생략 및 여러 기기에서 확인
- 비회원 정보·결과는 해당 브라우저 localStorage에만 제한적으로 저장
- 공개 운세에는 19세 제한 없음
- 19세 미만 복권 구매는 금지
- 만 14세 미만 계정 개인정보 저장은 별도 보호자 동의 없이 허용하지 않음
- 번호 생성은 복권 구매·당첨 보장 기능이 아님
- 데모 화면은 실제 사용자 데이터가 아님

기존 Privacy에 `비회원 정보는 저장되지 않는다`고 쓰여 있다면 localStorage 사용에 맞게 정확히 수정한다.

---

# 테스트와 인수 조건

## 24. 운세 테스트

- 회원 동일 날짜 동시 요청에서도 DB row 1개
- 회원 같은 날 결과 재확인 시 동일 결과
- 다음 KST 날짜에 새 결과
- 프로필 수정 후 오늘 결과 유지, 다음 날 반영
- 비회원 같은 브라우저·같은 프로필·같은 날 동일 결과
- 비회원 다른 프로필은 각자 하루 한 결과
- localStorage 삭제/차단 시 graceful fallback
- 오래되거나 손상된 localStorage schema 복구
- repeat view가 신규 생성 analytics로 집계되지 않음
- 생년월일·성별 값·태어난 시각이 analytics에 없음

## 25. 날짜 입력 테스트

- 8자리 붙여넣기 분배
- 하이픈·점 포함 날짜 붙여넣기
- 한 자리 월·일 정규화
- 윤년 2월 29일
- 평년 2월 29일 오류
- 4월 31일 오류
- 미래 날짜 오류
- 19세 미만 날짜 정상 통과
- 아동 생년월일 정상 통과
- keyboard와 mobile numeric input

## 26. 번호 생성 테스트

- `/generate` 직접 진입 시 번호 미생성
- 직접 진입 CTA 클릭 후 6개 생성
- 번호 범위 1~45, 중복 없음
- direct source는 전체 무작위
- dream CTA source는 자동 시작 가능
- dream source의 고정 숫자 유지
- 재생성 전에는 DB 저장 또는 `numbers_generated` 이벤트 없음
- page view와 generation event 분리

## 27. 데모 테스트

- `/demo/*` 비로그인 접근 가능
- 실제 `/my/*` auth guard는 그대로 유지
- demo fixture에 개인정보 없음
- Supabase mutation 0건
- noindex/nofollow
- sitemap 제외
- robots disallow
- 일반 navigation에서 링크 없음
- analytics KPI 제외
- `ENABLE_PUBLIC_DEMO=false`에서 404
- 데모와 실제 페이지가 동일 presentational component를 사용

## 28. 품질 명령

저장소의 실제 script를 확인해 가능한 모든 검증을 실행한다.

- formatter
- lint
- typecheck
- 전체 unit/integration tests
- production build
- Playwright 또는 브라우저 smoke test
- SEO metadata/robots/sitemap 검사

기존 테스트를 삭제하거나 assertion을 약화해 통과시키지 않는다.

---

# 최종 보고 형식

## A. 제품 정책 결과

- 하루 1회가 정확히 무엇을 제한하는지
- 회원과 비회원의 차이
- 같은 날 재확인 동작
- 비회원 soft limit의 우회 가능성

## B. 변경 파일과 데이터 흐름

- 회원 DB 흐름
- 비회원 localStorage 흐름
- 생년월일 입력 component
- 번호 생성 state machine
- demo fixture/data adapter 구조

## C. 개인정보·연령정책

- 브라우저에 저장되는 항목
- 서버로 전송되지 않는 항목
- 공개 운세 제한 해제 범위
- 유지되는 미성년 복권 구매 경고
- 만 14세 미만 계정 정책

## D. 검증 결과

- 실행한 명령
- 테스트 통과 수
- build 결과
- 수동 확인한 desktop/mobile 흐름

## E. 공개 데모 URL

- 각 demo route와 재현 가능한 상태 URL
- GPT 또는 외부 리뷰어에게 전달할 안전한 링크
- 데모를 끄는 환경변수 방법

## F. 사용자에게 남은 작업

- 필요한 Vercel 환경변수
- commit/push 후 확인 항목
- 정책 문구 최종 확인 항목

로컬 테스트만 통과한 상태를 공개 배포 완료라고 말하지 마라. 모든 변경을 마친 뒤 commit/push하지 말고 diff와 권장 commit message를 제시한 후 승인을 기다려라.
