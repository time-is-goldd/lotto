# Claude Code 실행 프롬프트 — Luck Platform 출시 준비

아래 요청을 하나의 제품 개선 작업으로 수행해줘. 단순히 조언하거나 코드 예시만 제시하지 말고, 현재 저장소를 직접 분석한 뒤 필요한 코드를 실제로 수정하고 검증해줘.

## 0. 역할과 최종 목표

너는 이 프로젝트의 시니어 Product Engineer이자 한국 웹서비스 PM, CRO/UX, Technical SEO 담당자다.

목표는 Luck Platform을 전면 재개발하는 것이 아니라, 현재 기능을 활용해 아래 핵심 루프가 실제로 작동하는 출시 가능 버전으로 만드는 것이다.

> 꿈 검색 → 세부 꿈해몽 → 꿈 숫자로 번호 완성 → 번호 저장을 위한 로그인 → 토요일 당첨 결과 비교 → 재방문

완료 기준은 “화면이 예뻐졌다”가 아니다. 다음 네 가지가 충족되어야 한다.

1. 검색엔진이 올바른 프로덕션 URL로 모든 유효한 꿈해몽 페이지를 발견하고 이해할 수 있다.
2. 꿈해몽 검색 유입 사용자가 다음 행동인 번호 생성까지 자연스럽게 이동한다.
3. 생성 번호를 저장할 이유와 로그인 이점, 토요일에 다시 올 이유가 명확하다.
4. 번호 추천·개인정보·운영 주체에 관해 과장하거나 사용자를 오해시키지 않는다.

## 1. 확정된 제품 결정

- 공식 영문 브랜드: `Luck Platform`
- 한국어 표기: `럭플랫폼`
- 프로덕션 URL: `https://luckplatform.co.kr`
- 현재 공개 확인용 URL: `https://lucky-zeta-azure.vercel.app/`
- 핵심 타깃: 인상적인 꿈을 꾼 뒤 모바일로 “OO꿈 해몽”을 검색하고, 그 의미와 재미용 행운 숫자를 함께 얻고 싶은 한국 사용자
- 핵심 포지셔닝: `꿈을 번호로 바꾸고, 토요일 결과까지 기억해주는 서비스`
- 번호는 오락 목적이며 당첨을 예측하거나 당첨 확률을 높인다고 표현하지 않는다.
- 실제 AI 추론을 사용하지 않는 기능에는 `AI 추천`, `AI 번호` 같은 표현을 사용하지 않는다.
- 결제, 구독, 커뮤니티, 푸시 알림, 대규모 신규 기능은 이번 작업 범위가 아니다.

## 2. 추측하면 안 되는 운영 정보

아래 값이 저장소나 환경설정에 없고 플레이스홀더도 채워져 있지 않다면, 코드 수정 전에 이 세 가지만 한 번에 묶어 질문해라. 다른 사소한 선택은 현재 구조에 맞춰 합리적으로 결정하고 진행해라.

- 서비스 운영자/사업자 표시명: `{{LEGAL_OPERATOR_NAME}}`
- 개인정보·회원탈퇴 문의 이메일: `{{PRIVACY_CONTACT_EMAIL}}`
- 개인정보처리방침 시행일: `{{PRIVACY_EFFECTIVE_DATE}}`

가짜 사업자 정보, 이메일, 시행일을 만들어 넣지 마라. 입력을 기다리는 동안에도 운영 정보와 무관한 코드베이스 분석은 먼저 수행해도 된다.

## 3. 작업 방식

1. 먼저 `git status`, 디렉터리 구조, package manager, Next.js 버전과 App/Pages Router 여부, 데이터 원천, 인증·DB·분석 도구, 배포 설정을 확인해라.
2. 기존 변경사항과 사용자 코드를 임의로 되돌리거나 덮어쓰지 마라. 파괴적 명령을 사용하지 마라.
3. 아래 요구사항을 현재 아키텍처에 맞게 구현하되, 스택 교체나 대규모 리팩터링은 하지 마라.
4. 이미 잘 작동하는 기능은 재작성하지 말고 연결·문구·메타데이터·상태 피드백을 보완해라.
5. 필요한 DB 스키마 변경이 있다면 먼저 비파괴적 migration을 만들고, 기존 데이터 보존과 롤백 방법을 설명해라.
6. 새 dependency는 꼭 필요한 경우에만 추가하고, 기존 라이브러리로 해결 가능하면 재사용해라.
7. 구현을 시작하기 전에 발견한 구조, 수정 대상 파일, 작업 순서를 짧게 보고하되 승인을 기다리지 말고 진행해라. 단, 2절의 운영 정보만 예외다.
8. 각 우선순위 묶음이 끝날 때마다 lint/typecheck/test/build를 실행해 회귀를 바로 잡아라.

---

# P0 — 출시 전 반드시 수정

## 4. 프로덕션 URL과 Technical SEO 정상화

현재 확인된 가장 큰 출시 차단 문제는 canonical, Open Graph, JSON-LD, robots.txt, sitemap.xml에 `http://localhost:3000`이 노출되고 sitemap이 실제 꿈 콘텐츠 전체를 포함하지 못하는 것이다.

다음을 구현해라.

### 4.1 단일 Site URL 설정

- `NEXT_PUBLIC_SITE_URL` 또는 서버 전용 동등 설정을 단일 source of truth로 만들어라.
- 프로덕션 기본값은 `https://luckplatform.co.kr`로 하되, 코드 곳곳에 URL을 중복 하드코딩하지 마라.
- Next.js의 `metadataBase`와 각 페이지 canonical, OG URL, JSON-LD URL, robots의 sitemap URL, sitemap의 `<loc>`가 모두 같은 설정을 사용하게 해라.
- 개발·테스트 환경에서도 생성된 공개 메타데이터에 `localhost:3000`이 섞이지 않도록 테스트해라.
- 전체 저장소를 검색해 공개 출력에 남는 `localhost`, 이전 Vercel URL, `Lucky Platform` 오표기를 제거하거나 의도된 개발 설정인지 분리해라.

### 4.2 메타데이터

- Home에도 self-referencing canonical을 추가해라.
- Home, 번호 생성, 꿈해몽 메인, 카테고리, Parent Dream, Situation Dream, 오늘의 행운, 서비스 소개, 이용약관, 개인정보처리방침에 적절한 canonical을 설정해라.
- 로그인, 온보딩, 개인 기록 및 결과 같은 비공개/개인화 페이지는 `noindex, nofollow`를 유지하거나 추가해라.
- 각 공개 페이지에는 정확히 하나의 명확한 H1이 있어야 한다.
- title과 description은 페이지마다 고유해야 하며 markdown 기호가 들어가면 안 된다.
- description은 본문을 무작정 잘라 중간 문장에서 끝내지 말고 전용 요약 필드 또는 markdown을 제거한 문장 단위 요약으로 생성해라.
- Parent title 권장 형식: `{꿈 이름} 해몽: 의미·상황별 풀이·행운 숫자 | Luck Platform`
- Situation title은 실제 숫자가 노출될 때만 `행운 숫자`를 약속하고, 없으면 `{상황 꿈 이름} 해몽: 의미와 해석 | Luck Platform`처럼 정직하게 작성해라.
- Breadcrumb JSON-LD와 필요한 구조화 데이터의 URL도 프로덕션 절대경로를 사용하게 해라. 화면에 없는 FAQ나 평점을 구조화 데이터로 꾸며내지 마라.

### 4.3 robots와 sitemap

- robots는 공개 콘텐츠를 허용하고 `/my/`, `/login`, `/onboarding`, `/api/`, 내부 preview/debug 경로는 차단해라.
- robots의 sitemap 주소는 `https://luckplatform.co.kr/sitemap.xml`이 되어야 한다.
- sitemap을 하드코딩된 일부 목록이 아니라 실제 canonical 콘텐츠 데이터 원천에서 동적으로 생성해라.
- 현재 기대 데이터 규모는 Parent Dream 약 61개, Situation Dream 약 396개다. 저장소의 실제 유효 데이터 수를 계산하고, 모든 indexable 카테고리·Parent·Situation URL이 sitemap에 한 번씩 포함되도록 해라.
- 누락되었던 카테고리와 콘텐츠도 데이터에 존재한다면 포함해라.
- 중복 URL, localhost URL, private URL, query URL, 404 URL, 빈 페이지는 sitemap에서 제외해라.
- `lastModified`는 실제 수정 시각을 사용할 수 있을 때만 넣고, 매 빌드마다 전 페이지를 현재 시각으로 위조하지 마라.
- FAQ가 여전히 비어 있다면 sitemap에서 제외하고 noindex 처리해라. 적은 비용으로 실제 동작에 근거한 유용한 FAQ를 채울 경우에만 index와 FAQPage JSON-LD를 사용해라.

### 4.4 SSR과 crawlability

- 꿈해몽의 핵심 제목, 해석 본문, 내부 링크는 초기 HTML에 서버 렌더링되어야 한다.
- 검색 UI나 애니메이션 때문에 핵심 콘텐츠가 client-only가 되지 않게 해라.
- 모든 내부 탐색은 실제 `<a>`/Next `Link`로 제공하고 클릭 핸들러만 있는 div를 사용하지 마라.

## 5. 꿈해몽 렌더링과 콘텐츠 품질 오류 수정

실제 페이지에서 `##`가 그대로 보이는 raw markdown 문제가 확인되었다. 메타 description에도 `##`가 들어갔다.

- 신뢰할 수 있는 기존 markdown renderer가 있으면 재사용하고, 없으면 안전하게 렌더링 가능한 최소 의존성을 사용해 markdown을 HTML로 렌더링해라.
- 사용자 입력 markdown이 있을 가능성이 있다면 XSS 안전성을 보장해라.
- 제목 레벨을 페이지 H1 체계와 충돌하지 않게 정규화해라. 콘텐츠 내부 heading은 H2/H3부터 시작하게 해라.
- 카드 미리보기와 meta description에서는 markdown을 제거한 plain text만 보여라.
- 긴 본문은 모바일에서 문단, 소제목, 리스트, 강조, 행간이 명확하도록 typography를 보완해라.
- 빈 섹션 제목을 출력하지 마라. 특히 숫자가 없는데 `행운 숫자` H2만 보여주는 상태를 제거해라.
- 본문이 짧거나 서로 지나치게 비슷한 페이지를 대량으로 자동 재작성하지 마라. 대신 아래 콘텐츠 감사 도구를 추가해라.
  - 각 dream URL의 제목, 글자 수, 소제목 수, 요약 존재 여부, 숫자 존재 여부, 관련 링크 수, 중복/유사 문구 위험, index 상태를 점검한다.
  - 결과를 사람이 검토하기 쉬운 CSV 또는 Markdown 보고서로 출력한다.
  - 가장 얇거나 반복적인 페이지와 우선 편집 후보 30개를 표시한다.
- 명백히 비어 있거나 오류 상태인 페이지는 index시키지 마라. 다만 단순 글자 수만으로 정상 콘텐츠를 무더기 삭제하거나 noindex하지 말고 보고서와 명시적 기준을 남겨라.

## 6. 브랜드와 신뢰 문구 통일

- 사용자에게 보이는 브랜드, metadata, OG, JSON-LD, footer, 약관의 서비스명을 모두 `Luck Platform`으로 통일해라.
- 한국어 설명에서 필요하면 `럭플랫폼`을 병기하되 `Lucky Platform`, `Luck Platform`, `럭키 플랫폼`이 혼용되지 않게 해라.
- 실제로 단순 무작위 생성이라면 `AI가 추천하는 번호`를 삭제하고 `재미로 만드는 나만의 번호 조합` 또는 `무작위 번호 생성`으로 바꿔라.
- 번호 생성 화면과 관련 CTA 근처에 아래 취지의 짧은 고지 문구를 제공해라.

> 번호는 재미를 위한 무작위 조합이며, 당첨을 보장하거나 당첨 확률을 높이지 않습니다.

- 서비스 소개·FAQ·약관의 설명이 실제 구현과 서로 모순되지 않게 해라.
- 동행복권의 공식 판매·예측 서비스처럼 보이는 표현과 디자인을 피하고, `비공식 서비스`임을 필요한 위치에서 명확히 해라.
- 당첨 결과를 표시할 때 실제 데이터 원천과 확인 기준 시각이 있는 경우 화면에 표시해라. 확인되지 않은 결과에 `공식`이라는 말을 붙이지 마라.

## 7. 개인정보처리방침과 계정 삭제 경로

현재 개인정보처리방침은 카카오 식별자·닉네임·생년월일·선택 정보 등을 다룬다고 설명하지만, 실제 연락 채널과 운영자 정보가 없고 화면에서 직접 삭제할 수 없다고 되어 있다.

- 코드와 DB 스키마를 확인해 실제 수집 항목, 필수/선택 여부, 이용 목적, 보유 기간, 제3자 제공/처리위탁, 분석 도구, 쿠키, 파기 방법을 현재 동작과 일치시켜라.
- 운영자 표시명, 개인정보 문의 이메일, 시행일을 명확하게 노출해라.
- 이미 안전한 회원탈퇴/데이터 삭제 기능이 있다면 사용자가 찾기 쉬운 계정 화면에 연결하고, 없다면 이번 범위에서 안전하게 구현 가능한지 판단해라.
- 즉시 구현이 위험하면 최소한 검증된 문의 이메일을 통한 삭제 요청 절차, 처리 범위, 본인 확인 방법을 명시해라. 없는 기능을 있다고 쓰지 마라.
- 로그인 버튼 아래에 이용약관·개인정보처리방침 링크와 동의 취지를 간결하게 제공해라.
- 분석 도구를 새로 연결한다면 개인정보처리방침과 동의/옵트아웃 처리도 실제 동작에 맞게 함께 업데이트해라.

## 8. 빈 화면과 실현되지 않은 약속 제거

- Home의 `이번 주 인기`에 데이터가 없으면 빈 대형 섹션을 렌더링하지 마라.
- 대신 실제 데이터가 있을 때만 조건부 표시하거나, 관리 가능한 `많이 찾는 꿈` 링크 6~8개를 보여줘 내부 탐색에 기여하게 해라.
- 비어 있는 FAQ는 4.3 기준에 따라 채우거나 숨겨라.
- 아직 구현되지 않은 `1년 후 연간 리포트` 같은 먼 약속은 Home의 핵심 가치에서 제거해라.
- `자동 저장`, `자동 비교` 등 실제 코드가 보장하지 않는 표현을 전수 검사해 정확한 동작으로 고치거나 기능을 실제로 연결해라.

---

# P1 — 핵심 퍼널과 전환 개선

## 9. Home을 꿈 검색 중심으로 재정렬

전면 재디자인은 하지 말고 현재 컴포넌트와 디자인 토큰을 활용해 정보 순서와 CTA를 바꿔라.

### 권장 Hero 문구

H1:

> 어젯밤 꿈, 무슨 뜻일까요?

설명:

> 꿈의 의미를 확인하고, 꿈에서 얻은 행운 숫자로 이번 주 번호까지 만들어보세요.

Hero의 가장 강한 행동은 꿈 검색이어야 한다.

- 검색 input placeholder: `예: 돼지꿈, 이가 빠지는 꿈, 지갑을 잃어버린 꿈`
- Primary CTA: `꿈 해몽 찾기`
- Secondary CTA: `바로 번호 생성`
- 검색창 아래에는 `많이 찾는 꿈` 4~6개를 짧은 링크로 제공해 검색어를 몰라도 시작할 수 있게 해라.
- 기능 카드 순서는 `꿈해몽 → 번호 생성 → 오늘의 행운 → 당첨확인 → 기록`으로 조정해라.
- 각 카드에는 기능명이 아니라 결과 중심의 한 문장과 명확한 동사를 사용해라.
- Home 상단만 보고도 “꿈을 해석하고 관련 숫자로 번호를 만들 수 있는 서비스”임을 5초 안에 이해할 수 있어야 한다.
- 신뢰 고지는 보이되 Hero를 압도하지 않도록 번호 기능 주변이나 짧은 안내 영역에 둬라.

## 10. 실제 꿈 검색 구현

현재 `/dream`에는 검색 input이 없다. Parent와 Situation 전체를 대상으로 빠르고 단순한 검색을 구현해라.

- `/dream` 상단과 Home Hero에서 동일한 검색 경험을 재사용해라.
- Parent 이름, Situation 이름, 대표 키워드/별칭을 검색 대상으로 삼아라.
- 공백·대소문자·기본적인 한국어 표현 차이를 정규화해라. 무리한 형태소 분석이나 외부 검색 인프라는 도입하지 마라.
- 입력 중 상위 5~8개 autocomplete 결과를 보여주고, 각 결과가 대표 꿈인지 세부 상황인지 구분해라.
- 키보드 탐색, Enter, Escape, focus, screen reader label을 지원해라.
- 결과 없음 상태에서는 카테고리와 인기 꿈 링크로 이어줘라.
- 검색어 자체의 동적 query 결과 페이지는 중복 SEO 페이지로 index하지 마라.
- 검색 선택 후 canonical Parent/Situation URL로 이동시켜라.

## 11. 꿈해몽 Landing Page 전환 구조

검색엔진에서 Situation Dream으로 바로 들어오는 사용자를 기준으로 구성해라.

권장 순서:

1. breadcrumb
2. H1과 1~2문장 즉답 요약
3. 상세 의미/상황별 해석
4. 핵심 해석 요약 카드
5. 이 꿈에서 가져온 재미용 숫자
6. `이 꿈의 숫자로 번호 완성하기` CTA
7. 관련된 다른 상황 꿈 3~6개
8. Parent/카테고리로 돌아가는 링크
9. 짧은 오락 목적 고지

구현 조건:

- 답보다 CTA가 먼저 나오지 않게 해라. 먼저 검색 의도를 만족시킨 뒤 다음 행동을 제안해라.
- Parent 페이지는 지나치게 긴 61개 전체 목록 대신 검색·카테고리·관련 상황을 사용해 탐색 부담을 줄여라.
- 각 Situation 페이지에서 같은 Parent의 관련 상황 3~6개와 상위 Parent 링크를 실제 링크로 제공해라.
- 관련 링크 anchor는 `자세히 보기`가 아니라 구체적인 꿈 이름을 사용해라.
- 콘텐츠가 없는 카드, 같은 자기 자신 링크, 깨진 경로, 중복 링크를 제거해라.

## 12. 꿈 숫자와 번호 생성의 관계를 투명하게 만들기

현재는 꿈 페이지와 연결되었다고 표시되지만 실제 생성 숫자와 화면의 추천 숫자 관계가 불명확하다. 다음 규칙으로 통일해라.

- Situation에 유효한 `luckyNumbers`가 있으면 우선 사용한다.
- 없으면 해당 Parent의 유효한 숫자 중 최대 3개를 상속한다.
- 숫자는 1~45, 중복 없음이라는 validation을 통과해야 한다.
- 꿈에서 가져올 수 있는 숫자가 1~3개라면 그 숫자는 고정하고, 나머지를 1~45의 무작위 중복 없는 숫자로 채워 총 6개를 만든다.
- 꿈 숫자가 하나도 없으면 일반 무작위 생성기로 동작하고 `꿈과 연결된 숫자`라고 표현하지 않는다.
- 꿈 숫자와 무작위 숫자를 UI에서 라벨 또는 시각적 범례로 구분한다.
- 도움말 문구 예시:

> 꿈 페이지에 연결된 재미용 숫자를 먼저 담고, 나머지는 1~45에서 무작위로 채웠어요.

- 같은 꿈에서 매번 꿈 숫자까지 전부 바뀌지 않게 하고, 무작위로 채우는 부분만 다시 생성되게 해라.
- URL query만 신뢰해 임의 숫자를 주입할 수 없도록 stable dream id/slug를 기준으로 서버 또는 검증된 데이터에서 숫자를 읽어라.
- 숫자 생성 애니메이션은 짧고 결과를 방해하지 않게 하며 `prefers-reduced-motion`을 존중해라.
- 생성 완료 시 성공 상태, 다시 생성, 저장 행동이 명확히 구분되어야 한다.

## 13. 생성 후 저장 CTA와 로그인 연결

생성 완료 후 passive text만 보여주지 말고 바로 행동 가능한 영역을 제공해라.

비회원 문구:

> 이 번호를 저장해둘까요?  
> 로그인하면 번호를 보관하고, 토요일 추첨 결과와 비교할 수 있어요.

CTA:

> 카카오로 번호 저장하기

요구사항:

- 로그인 이동 시 `next`와 `reason=save-number` 같은 안전한 내부 상태를 전달해라.
- 가능하면 로그인 전 생성한 번호를 안전한 session storage 등에 임시 보관하고, 로그인 성공 후 사용자 확인을 거쳐 저장해라.
- open redirect가 생기지 않도록 `next`는 내부 허용 경로만 인정해라.
- 이미 로그인한 사용자는 `이 번호 저장하기` 버튼과 성공/중복/실패 피드백을 받게 해라.
- 실제로 저장되지 않았는데 `자동 저장되었습니다`라고 말하지 마라.
- 저장된 번호에는 생성 시각, 회차 또는 대상 추첨일, 출처(`general`/`dream`), 연결된 dream id를 가능한 범위에서 보존해 향후 결과 비교에 사용해라.

## 14. 로그인 화면을 맥락별 전환 화면으로 개선

기본 로그인 페이지 문구:

> 카카오로 간편하게 시작하세요  
> 로그인하면 만든 번호를 저장하고, 토요일 추첨 결과와 비교할 수 있어요.

이점 목록:

- 생성한 번호 한곳에 보관
- 오늘의 행운 하루 한 번 확인
- 꿈·번호·당첨 결과를 함께 기록
- 다음 회차 결과 간편 확인

기본 CTA:

> 카카오로 계속하기

버튼 아래:

> 계속하면 이용약관과 개인정보처리방침에 동의하게 됩니다.

- `reason=save-number`, `reason=fortune`, `reason=check-result`, `reason=journal`에 따라 제목과 첫 번째 benefit을 맥락에 맞게 바꿔라.
- 결과 확인 gate 문구:

> 저장한 번호와 이번 회차 결과를 비교해보세요.  
> 카카오로 로그인하면 숫자를 다시 입력하지 않아도 됩니다.

- CTA: `카카오로 당첨 결과 확인하기`
- 카카오 로그인 자체의 보안 흐름은 기존 구현을 유지하고, callback/next 처리와 오류 상태를 테스트해라.
- 로그인하지 않겠다고 선택한 사용자가 Home이나 원래 공개 페이지로 쉽게 돌아갈 수 있게 해라.

## 15. 오늘의 행운을 ‘하루 한 번’ 이유로 만들기

로그인 전에는 문장 하나와 로그인 버튼만 두지 말고, 실제 결과 형태를 이해할 수 있는 비개인화 preview를 보여줘라.

- 예: 오늘의 전체운, 행운 색상, 행운 숫자, 행운 시간 카드의 제목만 보이고 값은 잠금 상태로 표시
- 문구: `오늘의 행운은 매일 자정 이후 새로 열려요.`
- CTA: `로그인하고 오늘의 행운 보기`
- 로그인 후 결과가 사용자별·날짜별로 하루 동안 일관되고 다음 날 갱신되는지 기존 로직을 확인하고 테스트해라.
- 매 새로고침마다 결과가 바뀐다면 사용자/날짜 기반으로 안정화해라.
- 날짜, 갱신 기준, 마지막 확인 상태를 명확히 보여줘라.
- 과도한 점술 확신 표현 대신 오락·자기성찰 관점의 문구를 사용해라.
- 새 기능을 늘리기보다 결과 하단에서 `오늘 번호 만들기`, `기록 보기`, `이번 주 저장 번호`로 기존 기능을 연결해라.

## 16. 기록과 당첨확인의 역할 정리

- `행운일기`는 새로운 장문 일기 기능으로 확장하지 마라.
- 사용자가 수동으로 글을 많이 써야 하는 기능보다 저장한 꿈·번호·오늘의 행운을 다시 보는 `기록` 역할을 우선해라.
- Home 카드와 모바일 nav에서는 `행운일기`보다 `기록`처럼 부담이 적은 라벨을 고려하되 페이지 제목에서는 기능을 충분히 설명해라.
- 당첨확인은 저장 번호가 있을 때 회차 결과와 자동 비교되는 핵심 재방문 장치로 연결해라.
- 결과 데이터가 아직 없으면 오류처럼 보이지 않게 다음 추첨일/업데이트 대기 상태를 보여줘라.
- 데이터가 수동 등록이라면 등록 시각과 출처 링크를 표시하고, 자동 실시간 공식 연동처럼 표현하지 마라.
- 저장 번호가 없는 사람에게는 `번호 만들기`로, 비회원에게는 맥락형 로그인으로 연결해라.

---

# P1 — 모바일·디자인·분석

## 17. 모바일 Bottom Navigation

현재 모바일 BottomNav에 꿈해몽이 빠져 있다. 핵심 유입 기능을 포함하도록 다음 5개를 우선 검토해라.

1. 홈
2. 꿈해몽
3. 번호
4. 오늘의 행운
5. 기록

- 현재 route에서 active state가 명확해야 한다.
- 아이콘은 기존 아이콘 시스템이 있으면 재사용하고 한 스타일로 통일해라.
- iPhone safe-area를 반영하고 본문/고정 CTA가 nav에 가리지 않게 충분한 bottom padding을 둬라.
- touch target은 최소 44px 수준으로 유지하고 5개 항목의 한국어 label이 작은 화면에서 잘리지 않게 해라.
- 당첨확인은 기록 또는 관련 화면 안에서 쉽게 접근하게 해라.

## 18. 모바일 UX 세부 조건

- 검색 input 글자 크기는 iOS 자동 확대가 생기지 않게 하고 clear 버튼과 키보드 제출을 지원해라.
- 긴 꿈해몽 본문은 한 줄 길이, 문단 간격, H2/H3 간격을 조정해 스캔하기 쉽게 해라.
- 꿈해몽 CTA는 답을 읽은 뒤 자연스럽게 나타나며 BottomNav와 겹치지 않아야 한다.
- 번호 6개는 320~375px 너비에서도 겹치거나 비정상 줄바꿈되지 않아야 한다.
- 생성 애니메이션 중 layout shift를 최소화해라.
- 오늘의 행운 카드가 좁은 화면에서 가로 스크롤을 강요하지 않게 해라.
- error/loading/empty/success 상태를 모바일에서 직접 확인해라.

## 19. ROI 높은 시각 보완

전면 재디자인, 로고 제작, 새로운 디자인 시스템 구축은 하지 마라.

- 기존 blue를 행동 색상으로 유지하고 gold는 행운 숫자/결과 강조에 제한적으로 사용해라.
- Hero, 검색, 핵심 CTA, 결과 카드의 시각적 위계를 강화해 generic template 느낌을 줄여라.
- 카드마다 목적 없는 장식은 추가하지 말고, 기존 icon library가 있으면 한 가지 선 스타일로 통일해라.
- 카드 padding, section spacing, 제목과 설명의 간격을 일관되게 정리해라.
- focus-visible, 명도 대비, aria-label, 상태 메시지의 screen reader 접근성을 확인해라.
- 외부 폰트 추가로 성능을 악화시키지 말고 현재 한국어 system font stack을 먼저 다듬어라.

## 20. 행동 분석 이벤트

기존 analytics가 있다면 중복 SDK를 추가하지 말고 재사용해라. 없다면 provider에 종속되지 않는 `trackProductEvent` 래퍼와 타입을 만들고, 실제 연결에 필요한 환경변수/설정 방법을 문서화해라. 설정되지 않았을 때 앱이 깨지거나 개인정보를 무단 전송하면 안 된다.

다음 이벤트를 최소 구현해라. 이벤트 이름과 핵심 property를 문서화하고 개인식별정보, 생년월일, 꿈 일기 원문은 보내지 마라.

| 이벤트 | 발생 시점 | 최소 속성 |
|---|---|---|
| `dream_search_submitted` | 꿈 검색 제출 | `query_length`, `result_count`, `location` |
| `dream_result_viewed` | Parent/Situation 진입 | `dream_id`, `dream_type`, `referrer_type` |
| `related_dream_clicked` | 관련 꿈 클릭 | `from_dream_id`, `to_dream_id` |
| `dream_number_cta_clicked` | 꿈→번호 CTA | `dream_id`, `dream_type` |
| `numbers_generated` | 6개 생성 완료 | `source`, `dream_number_count` |
| `save_number_clicked` | 저장 CTA | `authenticated`, `source` |
| `login_started` | 카카오 로그인 시작 | `reason` |
| `login_completed` | 로그인 성공 | `reason` |
| `number_saved` | 실제 DB 저장 성공 | `source`, `draw_id` |
| `fortune_viewed` | 오늘 결과 확인 | `is_return_visit` |
| `result_comparison_viewed` | 저장 번호 비교 확인 | `draw_id`, `saved_set_count` |

- `view`와 `success`를 혼동하지 마라. DB 저장 성공 전에 `number_saved`를 보내면 안 된다.
- 동일 렌더링으로 이벤트가 중복 전송되지 않게 해라.
- README 또는 `docs/analytics.md`에 핵심 funnel과 KPI 계산 방법을 적어라.
- North Star는 `꿈 페이지에서 시작해 번호를 저장하고 해당 회차 결과 비교까지 완료한 주간 사용자 수`다.

---

# 콘텐츠·SEO 운영 기반

## 21. 내부 링크와 index 품질

- `/dream`에서 61개 카드를 한 번에 나열하는 구조를 검색+카테고리+주요 꿈 링크 중심으로 정리하되, 모든 Parent가 crawl 가능한 카테고리/페이지 링크 구조 안에 남아야 한다.
- Parent → Situation, Situation → Parent, Situation → 관련 Situation, Home/Dream main → 주요 Parent의 링크 흐름을 확인해 orphan page를 없애라.
- 페이지 하단 관련 꿈은 태그/Parent/카테고리 등 설명 가능한 규칙으로 선정하고 무작위로 매번 바뀌지 않게 해라.
- 모든 396개 본문을 동일한 템플릿 문장으로 확장하지 마라.
- 반복 표현, 너무 짧은 해석, 고유 사례 부재는 코드로 숨길 문제가 아니라 편집 backlog로 남겨라.
- 감사 보고서에서 우선 수정 30개를 선정하되 기준을 함께 기록해라: 검색 가능성이 높은 주제, 현재 완성도, 내부 링크 허브 역할, 숫자/요약 누락, 문구 중복 위험.
- `README` 또는 `docs/content-quality.md`에 새 꿈 콘텐츠를 index하기 전 체크리스트를 추가해라.

권장 체크리스트:

- 검색어와 H1이 자연스럽게 일치하는가?
- 첫 2문장 안에 바로 답이 있는가?
- 해당 상황에 고유한 해석과 맥락이 있는가?
- 반복 템플릿 문장만으로 구성되지 않았는가?
- 관련 Parent/Situation 링크가 있는가?
- title/description이 고유하고 markdown이 제거되었는가?
- 숫자 섹션과 CTA가 실제 데이터와 일치하는가?
- 검토자와 수정일을 기록할 수 있는가?

## 22. Search Console 등록 준비

외부 계정 등록 자체는 하지 말고 코드와 문서만 준비해라.

- 네이버 서치어드바이저와 Google Search Console verification meta/env 설정 위치를 마련해라.
- `docs/search-launch-checklist.md`에 다음을 적어라.
  - 최종 도메인 연결 후 canonical/redirect 확인
  - `www`/non-www 및 HTTP→HTTPS 단일 301 정책
  - robots와 sitemap 제출
  - 대표 URL 수집 요청
  - 색인 수, 제외 사유, 검색어, CTR, 랜딩별 행동 데이터 주간 점검
  - 404/redirect/canonical 오류 확인
- Vercel preview deployment에는 가능하면 `noindex` 헤더/메타를 적용하고 프로덕션만 index되게 해라. 다만 프로덕션 도메인의 정상 indexing을 막지 않도록 환경 조건을 테스트해라.

---

# 명시적 제외 범위

## 23. 이번 작업에서 하지 말 것

다음 작업은 구현하지 말고 backlog에만 남겨라.

1. Premium 운세, 결제, 구독, paywall
2. 커뮤니티, 댓글, 랭킹, 사용자 간 공유 피드
3. AI 당첨 예측, 당첨 확률 분석, 복잡한 통계 추천
4. 푸시 알림·카카오 메시지 발송 같은 외부 리텐션 자동화
5. 대규모 브랜드 리뉴얼, 새 로고, 전면 디자인 시스템 교체
6. 꿈해몽 페이지 수 추가 생산
7. 396개 콘텐츠의 일괄 AI 재작성
8. 사용 데이터 없이 연간 리포트 같은 장기 기능 구축

이번 작업의 우선순위는 새 기능 수가 아니라 `검색 노출 → 꿈 답변 → 번호 생성 → 저장 → 결과 확인`의 연결 완성도다.

---

# 테스트와 인수 조건

## 24. 자동 검증

기존 테스트 프레임워크를 우선 사용하고, 없으면 핵심 회귀만 검증하는 가벼운 테스트를 추가해라.

최소한 다음을 자동 또는 재현 가능한 스크립트로 검증해라.

- 공개 HTML/metadata/robots/sitemap 어디에도 `http://localhost:3000`이 없다.
- canonical과 OG URL이 `https://luckplatform.co.kr` 기반이다.
- sitemap의 Parent/Situation 수가 실제 indexable 데이터 수와 일치한다.
- sitemap URL은 모두 중복 없이 200을 반환하거나 빌드 단계에서 유효 route임이 확인된다.
- private route는 sitemap에 없고 noindex다.
- Parent와 Situation 페이지는 H1이 하나이며 SSR HTML에 제목·본문·내부 링크가 있다.
- 화면과 metadata에 raw `##`, `**` 같은 markdown 토큰이 노출되지 않는다.
- 숫자는 항상 1~45, 총 6개, 중복 없음이다.
- dream source일 때 기존 꿈 숫자는 유지되고 나머지만 무작위로 채워진다.
- 일반 source일 때 꿈 연결 문구가 나오지 않는다.
- 로그인 `next`는 외부 URL을 허용하지 않는다.
- 로그인 전 생성 번호가 로그인 후 저장 흐름으로 안전하게 이어진다. 구현 불가하면 손실을 명확히 알리고 거짓 자동 저장 문구가 없어야 한다.
- 빈 popular/FAQ/숫자 섹션이 렌더링되지 않는다.
- 모바일 320, 375, 768px에서 nav/검색/번호/CTA가 겹치지 않는다.
- `prefers-reduced-motion`에서 번호 애니메이션이 축소 또는 제거된다.
- analytics success 이벤트는 실제 성공 후 한 번만 발생한다.

## 25. 수동 확인 시나리오

가능하면 Playwright 또는 브라우저 테스트로 다음 경로를 확인해라.

1. Home에서 `돼지꿈` 검색 → Parent 또는 적절한 결과 이동
2. Situation Dream 진입 → 답변 확인 → 관련 꿈 클릭
3. Situation Dream → `이 꿈의 숫자로 번호 완성하기`
4. 꿈 숫자와 무작위 숫자 구분 확인 → 다시 생성
5. 비회원 저장 CTA → 맥락형 Kakao 로그인 화면까지 이동
6. `/fortune` 비회원 preview와 로그인 CTA
7. `/my/journal/results` 비회원 맥락형 로그인 화면
8. 모바일 BottomNav의 5개 항목과 active state
9. About/Terms/Privacy에서 브랜드·운영 정보·고지 일치 확인
10. Home의 빈 섹션 제거와 첫 화면 메시지 확인

실제 카카오 외부 인증은 자격증명 없이 끝까지 강행하지 말고, callback 전후 내부 로직을 테스트 가능한 범위에서 검증해라.

## 26. 품질 명령

저장소에 정의된 정확한 script를 확인해 실행해라. 적어도 가능한 범위에서 다음을 모두 통과시켜라.

- formatter
- lint
- typecheck
- unit/integration tests
- production build
- 핵심 브라우저 smoke test
- SEO audit/content audit script

실패를 숨기거나 테스트를 삭제해 통과시키지 마라. 기존의 unrelated 실패가 있다면 이번 변경 때문인지 구분해서 보고해라.

---

# 최종 보고 형식

작업을 모두 끝낸 뒤 아래 순서로 보고해라.

## A. 결론

- 출시 차단 문제가 해결되었는지 `YES/NO`
- 아직 실제 사용자를 받기 전에 남은 blocker가 있는지

## B. 변경사항

- P0, P1로 나눠 사용자 행동에 어떤 변화가 생겼는지 설명
- 주요 수정 파일과 역할
- DB migration이나 환경변수 변경

## C. SEO 검증 결과

- 실제 Parent/Situation/indexable URL 수
- sitemap 총 URL 수
- canonical/robots/SSR/structured data 검사 결과
- localhost/이전 도메인 잔존 여부

## D. Funnel 검증 결과

- Home → dream search
- dream → generate
- generate → save/login
- login → return/save
- saved number → result comparison

각 연결을 `완료 / 부분 완료 / 미완료`로 표시해라.

## E. 테스트 결과

- 실행한 명령
- 통과/실패 수
- 브라우저로 확인한 화면 크기와 시나리오

## F. 내가 직접 해야 하는 일

코드 밖의 작업만 체크리스트로 남겨라. 예:

- Vercel 환경변수 입력
- 최종 도메인 DNS 연결
- Kakao redirect URI 추가
- 네이버 서치어드바이저/Google Search Console 등록
- 운영자 정보 최종 확인
- 분석 도구 키 입력

## G. 의도적으로 미룬 일

- 23절 제외 범위와 콘텐츠 수동 편집 backlog
- 우선 편집할 꿈 콘텐츠 30개 보고서 위치

마지막으로 “다음 기능을 무엇을 만들까?”를 제안하지 말고, 현재 핵심 루프에서 아직 약한 단 하나의 연결과 그 근거를 말해라.

---

# 최종 주의사항

- 숫자·운세를 과학적 예측처럼 포장하지 마라.
- 실제 구현을 확인하지 않고 자동 저장, 공식 연동, 개인화, 삭제 완료를 약속하지 마라.
- SEO를 위해 문장을 반복 복제하거나 페이지를 늘리지 마라.
- 디자인 완성도 때문에 P0 수정과 출시를 지연시키지 마라.
- 기존 사용 데이터와 인증 흐름을 보존해라.
- 설명만 하고 멈추지 말고, 수정·검증·최종 보고까지 완료해라.
