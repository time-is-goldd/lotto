# Claude Code 실행 프롬프트 — 임시 프로덕션 도메인·비회원 운세·운세 공개 UX 수정

현재 Luck Platform 저장소에서 아래 작업을 실제로 구현하고 검증해줘. 설명이나 코드 예시만 제시하지 말고, 현재 코드와 기존 미커밋 변경사항을 먼저 확인한 뒤 안전하게 수정해라.

## 0. 이번 작업의 기준

- 현재 사용자가 실제로 공개해 둘 임시 프로덕션 URL: `https://lotto-blue-sigma.vercel.app`
- 최종 구매 예정 도메인 `luckplatform.co.kr`은 아직 사용하지 않는다.
- 공식 브랜드는 `Luck Platform`으로 유지한다.
- 이번 목표는 다음 세 가지다.
  1. 사이트의 모든 공개 URL 기준을 현재 Vercel 도메인으로 통일
  2. 로그인하지 않은 사용자도 개인정보를 직접 입력해 오늘의 맞춤 운세 확인 가능
  3. 로그인 사용자의 운세가 진입 즉시 노출되지 않고, 명확한 버튼과 짧은 공개 애니메이션 뒤 나타나도록 개선

## 1. 기존 구현을 먼저 보존해라

이전 작업에서 아래 기능이 구현되었다고 보고받았고, 현재 공개 사이트에서도 일부를 직접 확인했다. 이를 재작성하거나 되돌리지 말고 회귀 테스트만 추가해라.

- 개인정보처리방침 시행일 `2026-08-18`
- 모바일 BottomNav 5개 항목: 홈·꿈해몽·번호생성·운세·다이어리
- OAuth `next`/`reason` 쿠키 왕복과 `isSafeNextPath` open redirect 방어
- 로그인 페이지의 `save-number`·`fortune`·`check-result`·`journal` 맥락별 문구
- 꿈 숫자와 무작위 숫자를 실제로 결합하는 `lib/logic/dreamNumbers.ts`
- 꿈 숫자 고정 및 무작위 부분만 재생성하는 동작
- 기존 analytics provider abstraction과 이벤트 타입
- 기존 589개 테스트 및 새로 추가된 테스트

작업 시작 시 반드시 다음을 먼저 해라.

1. `git status`와 전체 diff를 확인한다.
2. 기존 미커밋 변경사항이 이번 작업과 겹치는지 구분한다.
3. 사용자 변경을 reset, checkout, stash, discard하지 않는다.
4. 현재 branch, Vercel production branch, 실제 배포 alias가 어떤 commit 또는 working tree를 바라보는지 확인 가능한 범위에서 기록한다.
5. 현재 구조와 테스트를 파악한 뒤 짧은 구현 순서를 제시하고 바로 진행한다.

---

# P0. 현재 Vercel URL을 임시 프로덕션 기준 URL로 설정

## 2. 단일 Site URL

현재는 `https://lotto-blue-sigma.vercel.app`을 실제 공개 도메인으로 사용한다.

- 기존 `getSiteUrl()`, `metadataBase`, `NEXT_PUBLIC_SITE_URL` 또는 동등한 중앙 설정을 찾아 단일 source of truth를 유지해라.
- 임시 프로덕션 값은 정확히 `https://lotto-blue-sigma.vercel.app`으로 설정해라.
- 코드 여러 곳에 이 URL을 반복 하드코딩하지 마라.
- `.env.example`, 배포 문서, 필요한 환경변수 설명을 함께 갱신해라.
- 최종 도메인을 구입하면 환경변수 한 번만 바꿔 전체 canonical·OG·JSON-LD·robots·sitemap이 전환되도록 유지해라.
- 지금은 `luckplatform.co.kr`로 redirect하거나 canonical을 보내지 마라.
- Vercel 환경변수 변경 권한이 없다면 코드와 문서는 완료하고, 사용자가 Vercel에서 입력할 정확한 key/value만 최종 보고에 적어라.

## 3. 공개 URL 전수 검사

현재 공개 사이트를 직접 확인했을 때 다음 상태였다.

- Home `/`에는 canonical이 없음
- 꿈해몽 메인 `/dream`에도 canonical이 없음
- `/fortune`, `/about`, `/privacy`, Parent Dream에는 현재 Vercel URL canonical이 있음
- Home의 WebSite JSON-LD URL은 현재 Vercel URL을 사용 중

누락과 불일치를 수정해라.

- Home, 꿈해몽 메인, 카테고리, Parent Dream, Situation Dream, 번호 생성, 오늘의 행운, About, Terms, Privacy, 실제 내용이 있는 FAQ에 self-referencing canonical을 제공한다.
- 번호 생성의 query variant는 canonical을 `/generate`로 통일한다.
- metadataBase, OG URL, JSON-LD, breadcrumb URL, robots의 Sitemap, sitemap의 모든 `<loc>`를 같은 Site URL에서 생성한다.
- 로그인·온보딩·개인 기록·개인 당첨 결과 등 private route는 sitemap에서 제외하고 noindex를 유지한다.
- Vercel preview deployment와 production deployment를 구분하되, 현재 `lotto-blue-sigma.vercel.app` production alias를 실수로 noindex하지 마라.
- 런타임 출력에 `http://localhost:3000` 또는 아직 구매하지 않은 최종 도메인이 남지 않게 테스트한다.

## 4. 도메인 인수 조건

- `/`, `/dream`, `/fortune`, 샘플 Parent 2개, 샘플 Situation 2개에서 canonical이 존재한다.
- canonical, OG, JSON-LD URL은 모두 `https://lotto-blue-sigma.vercel.app`으로 시작한다.
- robots.txt의 sitemap 주소도 같은 host를 사용한다.
- sitemap에 다른 host가 섞이지 않는다.
- `npm run build` 결과의 공개 metadata에 localhost가 없다.
- 향후 최종 도메인 전환 방법을 기존 도메인 준비 문서에 간결하게 갱신한다.

---

# P0. 비회원도 오늘의 맞춤 운세 이용 가능

## 5. `/fortune` 로그인 장벽 제거

현재 비회원에게는 다음 문구와 로그인 링크만 보인다.

> 로그인하면 생년월일을 바탕으로 오늘의 행운을 확인할 수 있어요.  
> 로그인하고 확인하기

이를 비회원 입력 폼으로 교체해라. 로그인은 필수가 아니라 편의성과 저장을 위한 선택이어야 한다.

### 비회원 첫 화면 권장 문구

H1:

> 오늘의 행운

설명:

> 로그인 없이 생년월일을 입력하고 오늘의 맞춤 운세를 확인해보세요.

로그인 유도 보조 문구:

> 카카오로 로그인하면 다음부터 생년월일을 다시 입력하지 않아도 돼요.

보조 링크:

> 로그인하고 간편하게 보기

로그인하지 않아도 폼 작성과 결과 확인을 끝까지 완료할 수 있어야 한다.

## 6. 비회원 입력 폼

필드는 필요한 것만 사용해라.

- 생년월일: 필수
- 성별: 화면에는 제공하되 `남성`, `여성`, `선택 안 함`을 허용한다. 현재 개인정보처리방침과 온보딩에서 성별이 선택 항목이므로 강제로 필수화하지 않는다.
- 태어난 시각: 선택
- 이름, 전화번호, 이메일, 주소 등 운세 계산에 필요 없는 항목은 추가하지 않는다.

폼 UX 조건:

- 모바일에서 날짜·시간 입력이 편해야 한다.
- label을 명시하고 placeholder에만 의존하지 않는다.
- 미래 날짜, 잘못된 날짜, 만 나이 경계값을 정확히 검증한다.
- 현재 정책이 만 19세 이상 이용 제한이므로 비회원도 동일한 기준을 적용한다.
- 만 나이는 기존 한국식 나이가 아니라 생일 기준 만 나이로 계산한다.
- 날짜 검증은 서비스 기준 시간대 `Asia/Seoul`에서 일관되게 처리한다.
- 오류는 필드 가까이에 표시하고 첫 오류로 focus를 이동한다.
- 제출 버튼 문구는 `오늘의 맞춤 운세 보기`로 한다.
- 제출 중 중복 클릭을 막고 loading/error/retry 상태를 제공한다.

폼 아래 개인정보 안내:

> 비회원이 입력한 정보는 오늘의 운세를 만드는 데만 사용되며 계정이나 데이터베이스에 저장되지 않습니다.

이 문구는 실제 구현이 정말 그러할 때만 사용한다. 서버 로그, DB, analytics에 raw 생년월일·성별·태어난 시각을 남기지 마라.

## 7. 운세 계산 로직

추가 입력을 받기만 하고 계산에 사용하지 않는 기만적인 구현을 하지 마라.

- 기존 오늘의 행운 계산 로직을 찾아 회원·비회원이 함께 사용하는 순수 domain function으로 분리하거나 재사용해라.
- 입력 계약 예시: `targetDate`, `birthDate`, `gender?`, `birthTime?`.
- 생년월일은 실제 결과 seed에 사용해야 한다.
- 성별이나 태어난 시각을 입력한 경우 실제 seed 또는 개인화 요소에 반영한다.
- 선택하지 않은 값은 임의의 기본 성별이나 정오로 위장하지 말고 명시적인 `unknown`/`omitted` 상태로 처리한다.
- 동일한 사용자 입력과 동일한 한국 날짜에는 새로고침해도 같은 결과가 나와야 한다.
- 다음 한국 날짜에는 새 결과가 나와야 한다.
- 비회원과 회원이 같은 입력을 사용하면 기본 운세 결과도 일관되어야 한다.
- 외부 AI 분석을 사용하는 것처럼 표현하지 않는다.
- 결과는 오락·자기성찰용이며 미래나 재정 결과를 보장하지 않는다는 짧은 고지를 유지한다.

## 8. 비회원 데이터 처리

- 비회원 입력값과 결과를 Supabase 사용자 테이블이나 운세 기록 테이블에 저장하지 마라.
- URL query에 생년월일·성별·태어난 시각을 넣지 마라.
- analytics event property에도 raw 값이나 이를 복원할 수 있는 hash를 보내지 마라.
- 페이지를 벗어나면 다시 입력해야 하는 것이 기본 동작이다. 이것이 로그인 편의성의 이유다.
- OAuth 후 입력 폼을 미리 채우는 흐름을 구현한다면 sessionStorage에만 잠시 보관하고, 로그인 후 사용자가 확인·동의한 다음 계정 정보로 저장해라. 조용히 자동 저장하지 마라.

## 9. 결과 뒤 로그인 전환

비회원에게 결과를 먼저 충분히 보여준 뒤 로그인 가치를 제시해라. 결과를 가리거나 가입을 강제하지 마라.

결과 하단 카드 문구:

> 다음부터는 입력 없이 바로 확인하세요  
> 카카오로 로그인하면 생년월일과 선택 정보를 저장하고, 매일 같은 기준으로 오늘의 행운을 확인할 수 있어요.

CTA:

> 카카오로 로그인하고 정보 저장하기

조건:

- `next=/fortune&reason=fortune` 흐름을 기존 안전한 OAuth 로직에 연결한다.
- 로그인만 했다고 비회원 입력정보를 자동 저장하지 않는다.
- 저장 전 어떤 항목이 저장되는지 보여주고 사용자가 확인하게 한다.
- 기존 프로필이 있다면 덮어쓰기 전에 충돌을 알려준다.
- `나중에 할게요`를 선택해도 결과를 계속 볼 수 있어야 한다.

---

# P0. 로그인 사용자의 운세 공개 경험 개선

## 10. 현재 문제

사용자 보고에 따르면 로그인 후 `/fortune`에 들어가면 버튼 클릭이나 공개 과정 없이 운세 결과가 즉시 나타난다.

로그인 상태는 공개 사이트 세션에서 직접 확인할 수 없으므로 이 부분은 코드·테스트·개발용 인증 fixture를 통해 검증해라. 추측으로 화면만 바꾸지 마라.

## 11. 명시적인 상태 흐름

운세 화면을 최소한 다음 상태로 분리해라.

1. `ready` — 오늘의 운세를 보기 전
2. `revealing` — 버튼 클릭 후 짧은 공개 애니메이션
3. `result` — 결과 표시
4. `error` — 생성 또는 조회 실패

로그인 사용자가 `/fortune`에 처음 들어오면 저장된 프로필이 완성된 경우에도 결과를 즉시 노출하지 않는다.

권장 문구:

> 저장된 생년월일을 바탕으로 오늘의 행운을 준비했어요.

Primary CTA:

> 오늘의 맞춤 운세 보기

보조 행동:

> 내 정보 수정

버튼을 클릭한 뒤에만 운세를 공개한다.

## 12. 공개 애니메이션

- 1.2~1.8초 정도의 짧은 reveal animation을 사용한다.
- 새 animation library를 추가하지 말고 기존 CSS/animation 도구를 우선 사용한다.
- 예: 운세 카드의 은은한 shimmer, 행운 색상 전환, 번호 카드가 순서대로 나타나는 방식.
- `AI가 분석 중`, `당첨 가능성을 계산 중`처럼 실제보다 과장된 문구를 사용하지 않는다.
- loading 문구는 `오늘의 행운 카드를 준비하고 있어요` 정도로 정직하게 작성한다.
- CTA를 disabled 처리해 중복 요청과 중복 DB row 생성을 막는다.
- 실제 응답이 늦으면 애니메이션 종료 후에도 loading을 유지하고, 실패하면 명확한 재시도 UI를 보여준다.
- `prefers-reduced-motion` 사용자는 동작을 최소화하되 버튼 클릭 후 결과가 나타나는 정보 구조는 유지한다.
- `aria-live`로 상태를 알리고 결과가 나타나면 결과 heading으로 focus를 이동한다.

## 13. 같은 날 결과의 안정성

- 공개 애니메이션은 결과를 새로 뽑는 척하는 장치가 아니라 이미 결정된 오늘의 결과를 보여주는 presentation이다.
- 동일한 계정과 같은 한국 날짜에는 결과가 바뀌지 않아야 한다.
- 기존 daily result가 있으면 재사용하고 중복 저장하지 않는다.
- 오늘 처음 생성하는 경우에도 idempotent하게 한 건만 저장한다.
- 페이지 재진입 시 결과를 다시 가리고 `오늘의 운세 다시 보기` 버튼을 제공할 수 있다.
- 재확인 애니메이션은 0.6~1.0초로 짧게 해 반복 방문을 방해하지 않는다.
- 다음 날에는 다시 `오늘의 맞춤 운세 보기` 상태에서 새 결과를 공개한다.

## 14. 프로필 불완전 상태

- 로그인했지만 생년월일이 없다면 결과를 만들지 말고 같은 입력 폼을 보여준다.
- 기존 성별·태어난 시각이 없더라도 생년월일만 있으면 결과 확인은 가능해야 한다.
- 사용자가 선택 정보를 추가하면 그 다음 결과부터 실제 계산에 반영한다.
- 프로필 수정과 오늘 결과 변경의 규칙을 명확히 정한다. 이미 오늘 결과가 생성되었다면 조용히 여러 결과를 만들지 말고, `내일부터 변경된 정보가 반영됩니다`처럼 일관된 정책을 사용한다.

---

# P1. 실제 배포본에서 추가로 확인된 결함

아래 문제는 `https://lotto-blue-sigma.vercel.app`을 직접 탐색해 확인했다. “이미 구현됐다”고 가정하지 말고 실제 렌더링 결과와 코드 양쪽을 확인해 수정해라.

## 15. Home의 과장·빈 콘텐츠·미구현 약속

현재 Home에 다음이 남아 있다.

- 번호 생성 카드: `AI가 추천하는 번호로 오늘의 행운을 시작해보세요.`
- 빈 `이번 주 인기` 섹션: `아직 인기 콘텐츠가 없어요`
- `1년치 행운 기록을 연말 결산 리포트로 받아볼 수 있어요`
- Hero가 여전히 번호 생성 중심이며 꿈 검색 퍼널을 설명하지 못함

수정 요구:

- 실제 AI를 사용하지 않으므로 `AI가 추천하는 번호`를 `꿈 숫자와 무작위 숫자로 나만의 번호를 만들어보세요` 또는 정확한 동작 문구로 변경한다.
- 데이터가 없는 인기 섹션은 렌더링하지 않는다. 관리 가능한 실제 주요 꿈 링크가 있다면 `많이 찾는 꿈`으로 대체한다.
- 구현되지 않은 연말 결산 리포트 약속을 삭제한다.
- Home Hero에 기존 `/dream` 검색 컴포넌트를 재사용해 꿈 검색을 primary action으로 둔다.
- Secondary CTA로 `바로 번호 생성`을 제공한다.
- 기능 카드 순서를 `꿈해몽 → 번호 생성 → 오늘의 행운 → 당첨확인 → 기록`으로 조정한다.
- 전면 재디자인은 하지 않는다.

권장 Hero 문구:

> 어젯밤 꿈, 무슨 뜻일까요?  
> 꿈의 의미를 확인하고, 꿈에서 얻은 행운 숫자로 이번 주 번호까지 만들어보세요.

## 16. 꿈해몽 메인 카드의 raw markdown과 과도한 길이

현재 `/dream`의 Parent 카드 설명에는 `##`가 그대로 노출되고 긴 본문 전체가 카드 accessible name과 화면에 들어간다. Parent 상세 본문에서는 markdown이 정상 렌더링되지만 목록 카드에서는 처리되지 않는다.

- 카드 전용 excerpt helper를 만들거나 기존 markdown plain-text 변환기를 재사용한다.
- `##`, `**`, link syntax 등 markdown 토큰을 제거한다.
- 첫 의미 있는 문장 또는 80~140자 정도의 짧은 고유 요약만 표시한다.
- 문장 중간에서 자르지 않는 것을 우선하고, CSS line clamp로 2~3줄을 넘지 않게 한다.
- link의 accessible name이 수백 자가 되지 않게 제목·카테고리·짧은 요약만 포함한다.
- 현재 작동하는 Parent/Situation autocomplete 검색은 보존한다.
- 전체 Parent 링크의 crawlability는 유지한다.

## 17. meta description 문장 중간 절단

확인한 `돼지꿈` description은 `단순히 돼지를 보기만 했는지, 돼지가 직접 다`에서 끊긴다.

- markdown 제거 후 무조건 글자 수로 자르지 말고, 적정 길이 안의 마지막 완결 문장에서 끝내는 공통 helper를 사용한다.
- 전용 summary가 있으면 그것을 우선 사용한다.
- title/description에 `##`가 들어가지 않게 테스트한다.
- Parent 3개와 Situation 3개를 샘플링해 고유성과 문장 완결성을 테스트한다.

## 18. 빈 FAQ가 index되는 문제

현재 `/faq`는 `아직 등록된 FAQ가 없어요`만 표시하면서 `index, follow`다.

둘 중 하나를 선택해 실제로 완료해라.

권장안:

- 현재 기능에 근거한 FAQ 5~7개를 작성한다.
- 번호 생성 방식, 꿈 숫자 의미, 비회원 운세 입력정보 처리, 로그인 이점, 결과 비교 방식, 당첨 보장 불가, 계정 삭제 방법을 다룬다.
- 실제 화면에 보이는 질문·답변과 동일할 때만 FAQPage JSON-LD를 추가한다.

대안:

- FAQ를 지금 채우지 않을 경우 noindex 처리하고 sitemap 및 About의 `FAQ / 가이드 제공` 약속에서 제거한다.

빈 상태를 index한 채 두지 마라.

## 19. 번호 생성 후 로그인 CTA

현재 꿈 연동 생성은 실제로 정상 동작한다. `돼지꿈`에서는 꿈 숫자 `3, 7, 12`가 유지되고 나머지만 무작위로 채워지며 범례도 보인다. 이 로직은 보존해라.

다만 생성 완료 후 화면은 `로그인하면 생성한 번호가 다이어리에 자동으로 기록돼요`라는 문장 속 `로그인` 링크에 의존한다.

- 실제 저장 흐름이 OAuth 복귀 후 작동하는지 통합 테스트한다.
- 자동 저장이 실제로 보장될 때만 `자동으로 기록`이라고 표현한다.
- passive link 대신 결과 바로 아래에 명확한 저장 카드와 CTA를 제공한다.

권장 문구:

> 이 번호를 저장해둘까요?  
> 로그인하면 번호를 보관하고, 토요일 추첨 결과와 비교할 수 있어요.

CTA:

> 카카오로 번호 저장하기

## 20. About·Privacy 문구 동기화

현재 About은 오늘의 행운을 `로그인 후`만 이용할 수 있다고 설명한다. 비회원 기능 구현 후 반드시 갱신해라.

- About: 비회원은 직접 입력해 확인할 수 있고, 회원은 저장된 정보로 반복 입력 없이 이용한다는 차이를 설명한다.
- Privacy: 비회원 입력값이 DB에 저장되지 않는 실제 처리 방식을 명시한다.
- 회원 프로필의 생년월일·성별·태어난 시각 처리와 비회원 일시 처리의 차이를 구분한다.
- analytics abstraction이 실제 외부 전송을 하지 않는 상태라면 현재 `분석 도구를 사용하지 않는다`는 문구와 모순 없는지 확인한다.
- 외부 analytics를 실제 연결했다면 정책을 실제 동작에 맞춰 갱신한다.

---

# 분석 이벤트

## 21. 운세 funnel 이벤트

기존 provider abstraction을 재사용하고 PII를 보내지 마라. 최소 이벤트:

| 이벤트 | 발생 시점 | 허용 속성 |
|---|---|---|
| `fortune_form_viewed` | 비회원/프로필 미완성 폼 노출 | `auth_state` |
| `fortune_form_submitted` | 유효한 폼 제출 | `auth_state`, `has_gender`, `has_birth_time` |
| `fortune_reveal_started` | 공개 버튼 클릭 | `auth_state`, `is_repeat_view` |
| `fortune_revealed` | 결과가 실제 표시됨 | `auth_state`, `is_repeat_view` |
| `fortune_login_cta_clicked` | 비회원 결과 후 로그인 CTA | `location` |
| `fortune_profile_saved` | 사용자 확인 후 프로필 저장 성공 | 선택 정보 존재 여부만 |

- 생년월일, 성별 값, 태어난 시각, 운세 문구 원문, 사용자 식별자는 property로 보내지 않는다.
- React rerender로 중복 전송되지 않게 한다.
- `fortune_revealed`는 animation 시작이 아니라 결과가 실제로 보인 뒤 한 번만 보낸다.
- 기존 `docs/analytics.md`의 funnel과 event status를 갱신한다.

---

# 테스트 및 인수 조건

## 22. 비회원 시나리오

1. 로그아웃 상태로 `/fortune`에 직접 접속한다.
2. 로그인 wall 없이 입력 폼이 보인다.
3. 생년월일만으로 제출 가능하다.
4. 성별 선택 안 함, 태어난 시각 미입력도 정상 동작한다.
5. 성별과 태어난 시각을 입력한 경우 실제 결과 seed가 달라진다.
6. 버튼 클릭 전에는 결과가 보이지 않는다.
7. 클릭 후 짧은 animation 뒤 결과가 나타난다.
8. 같은 날 같은 입력은 같은 결과다.
9. 비회원 입력값은 DB, URL, analytics에 남지 않는다.
10. 결과를 본 뒤에도 로그인을 강제하지 않는다.
11. 로그인 CTA의 이점이 `다음부터 입력하지 않아도 됨`으로 명확하다.
12. 만 19세 경계값과 미래 날짜가 정확히 검증된다.

## 23. 회원 시나리오

1. 완성된 프로필을 가진 로그인 사용자로 `/fortune`에 진입한다.
2. 결과가 즉시 노출되지 않고 `오늘의 맞춤 운세 보기` 버튼이 보인다.
3. 클릭 후 1.2~1.8초 reveal을 거쳐 결과가 나타난다.
4. 같은 날 다시 확인해도 결과는 바뀌지 않는다.
5. 같은 날 DB row가 중복 생성되지 않는다.
6. 다음 날에는 새 결과가 생성된다.
7. 프로필이 불완전하면 입력 폼으로 자연스럽게 보완할 수 있다.
8. 정보 수정 정책과 오늘 결과 반영 시점이 화면 문구와 일치한다.
9. reduced-motion 환경에서도 버튼→결과 순서는 유지된다.
10. OAuth `next=/fortune&reason=fortune` 복귀가 유지된다.

## 24. SEO·콘텐츠 시나리오

1. Home과 `/dream`을 포함한 공개 주요 페이지에 canonical이 있다.
2. 모든 공개 URL host가 현재 Vercel 도메인과 일치한다.
3. Home에 `AI가 추천하는 번호`가 없다.
4. 빈 인기 섹션과 연말 리포트 약속이 없다.
5. `/dream` 카드에 raw markdown과 본문 전체가 노출되지 않는다.
6. Parent description이 완결 문장으로 끝난다.
7. `/faq`는 실제 FAQ가 있거나 noindex다.
8. About과 Privacy가 새 비회원 운세 동작과 일치한다.

## 25. 자동 검증

저장소의 실제 script 이름을 확인해 가능한 항목을 모두 실행해라.

- formatter
- lint
- typecheck
- 전체 unit/integration tests
- production build
- 핵심 Playwright/browser smoke test
- canonical/robots/sitemap 검사

기존 테스트를 삭제하거나 assertion을 약화해 통과시키지 마라. 다음 단위 테스트를 반드시 포함해라.

- 만 19세 생일 전날/당일/다음 날
- 한국 날짜 자정 경계
- 생년월일만 입력한 deterministic result
- 선택 입력의 반영 여부
- 같은 입력·같은 날 동일 결과
- 다음 날 다른 결과
- 회원 daily result idempotency
- 운세 화면 상태 전이 `ready → revealing → result`
- reduced-motion branch
- 로그인 safe next 회귀
- 비회원 PII가 analytics payload에 들어가지 않는지

---

# 작업 완료 보고 형식

최종 답변은 아래 형식으로 작성해라.

## A. 구현 결과

- 임시 도메인 설정: 완료/부분 완료/미완료
- 비회원 운세: 완료/부분 완료/미완료
- 로그인 reveal UX: 완료/부분 완료/미완료
- 추가 발견 결함: 항목별 완료 상태

## B. 변경 파일과 핵심 로직

- 각 파일의 역할
- 운세 domain function의 입력과 deterministic 규칙
- guest/member 저장 차이
- animation state machine

## C. 개인정보와 데이터 저장

- 비회원 입력이 어디까지 이동하고 어디에도 저장되지 않는지
- 회원 입력이 언제 동의 후 저장되는지
- analytics payload에 포함되는 것과 제외되는 것

## D. 테스트 결과

- 실행한 모든 명령
- 통과한 테스트 수
- build 결과
- 확인하지 못한 외부 인증/배포 영역

## E. 배포 전 사용자 작업

- Vercel 환경변수에 넣을 정확한 key/value
- commit/push 후 확인해야 할 production URL
- 도메인을 최종 구매했을 때 바꿀 단일 설정

## F. 실제 배포 검증

- 현재 공개 URL에서 확인된 변경과 확인되지 않은 변경을 구분한다.
- 로컬 코드가 통과했다는 이유만으로 공개 배포 완료라고 말하지 마라.
- 배포 후 Home, `/dream`, `/fortune`, `/faq`, 꿈→번호→로그인 흐름을 다시 smoke test해야 한다.

이번 작업에서는 기존 변경을 포함해 커밋하거나 push하지 마라. 모든 수정과 검증을 끝낸 뒤 diff 요약과 권장 commit message를 제시하고 내 승인을 기다려라.

마지막으로 새 기능을 제안하지 말고, 이번 변경 후에도 `검색 유입 → 꿈 → 번호 저장 → 토요일 결과 확인`에서 가장 약한 연결 하나만 근거와 함께 보고해라.
