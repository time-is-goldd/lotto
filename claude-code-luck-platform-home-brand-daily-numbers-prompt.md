# Claude Code 실행 프롬프트 — Luck Platform 홈·브랜드·오늘의 세 조합 재구성

아래 작업을 현재 Luck Platform 저장소에 실제로 구현하고 검증해줘.

너는 이 작업에서 시니어 프로덕트 디자이너, CRO/UX 설계자, 디자인 시스템 엔지니어, 시니어 Next.js 프론트엔드 엔지니어 역할을 동시에 맡는다. 설치되어 있다면 `frontend-design` 스킬을 반드시 사용한다.

이번 작업은 새 사이트를 처음부터 만드는 일이 아니다. 이미 정상 동작하는 기능·데이터·인증·SEO URL을 보존하면서 프레젠테이션 레이어와 홈 정보 구조를 점진적으로 재구성하는 작업이다. 조언이나 목업만 제시하지 말고, 저장소 감사 → 기준 상태 확인 → 디자인 문서화 → 구현 → 테스트 → 화면 캡처 → 최종 보고까지 완료해라.

---

## 0. 프로젝트 배경

- 서비스명: `Luck Platform`
- 한국어 표기: `럭플랫폼`
- 현재 공개 기준 URL: `https://lotto-blue-sigma.vercel.app`
- 향후 공식 도메인: `luckplatform.co.kr`
- 핵심 타깃: 한국 모바일 사용자인 “꿈해몽 검색 사용자 중 꿈의 의미를 확인한 뒤 관련 행운 숫자와 번호 조합까지 재미로 이어가고 싶은 사람”
- 핵심 성장 흐름:
  - 검색 → 세부 꿈해몽 → 꿈 숫자 → 번호 생성 → 저장/로그인 → 토요일 결과 확인 → 재방문
- 홈의 단일 임무:
  - 방문자가 5초 안에 “꿈을 검색해 의미와 행운 숫자를 확인하는 서비스”라고 이해하고 꿈 검색을 시작하게 한다.
- 홈의 유일한 Primary Action:
  - 꿈 검색 제출
- 홈의 Secondary Action:
  - 꿈 없이 번호 생성 페이지로 이동

---

## 1. 작업 전 필수 원칙

1. 가장 먼저 `git status`, 전체 diff, 최근 변경 파일을 확인한다.
2. 사용자 또는 이전 세션의 미커밋 변경을 `reset`, `checkout`, `restore`, `stash`, 삭제하거나 덮어쓰지 않는다.
3. 현재 package manager, Next.js 구조, Tailwind 방식, 전역 CSS, 토큰, 공통 컴포넌트, 인증, Supabase schema/migration, analytics abstraction, 테스트 도구를 먼저 조사한다.
4. 저장소의 실제 scripts를 확인한 뒤 baseline lint, typecheck, test, build를 실행한다. 기존 실패가 있으면 이번 작업과 분리해 기록한다.
5. 가능하면 수정 전 홈과 번호 생성 화면을 390px 및 1440px에서 캡처한다.
6. 다음 기존 작업을 반드시 보존한다.
   - 카카오 OAuth의 안전한 `next/reason` 왕복
   - 로그인 reason별 맥락 문구
   - 꿈 숫자와 번호 생성의 실제 연결
   - 꿈 숫자를 고정하고 나머지만 생성하는 로직
   - 오늘의 행운 deterministic 결과 및 일일 정책
   - 기존 analytics provider/abstraction
   - 모바일 5탭 BottomNav
   - 개인정보처리방침 및 연령 정책
   - 로그인 후 화면 검토용 demo가 있다면 해당 구조와 `noindex` 정책
7. 기존 기능, 콘텐츠 데이터, 공개 URL, route slug, canonical/metadata 구조를 임의로 삭제하거나 바꾸지 않는다.
8. 디자인을 위해 무거운 dependency, UI kit, 애니메이션 패키지를 새로 추가하지 않는다. 현재 의존성으로 구현 가능한 범위를 우선한다.
9. 결과를 실질적으로 바꾸는 blocker만 질문한다. 그 외에는 아래 확정 명세대로 구현한다.
10. 이번 프롬프트에서 디자인 방향은 이미 승인됐다. 색만 다른 시안 3개를 다시 만들지 말고, 선택된 방향을 문서화한 뒤 구현한다.
11. 작업 완료 후 commit 또는 push하지 않는다. diff와 권장 commit message를 보고하고 승인받기 전 멈춘다.

---

## 2. 선택된 디자인 방향

### DESIGN THESIS

> 꿈의 여운을 담는 넉넉한 흰 여백 위에, 복권의 설렘은 절제된 딥레드 CTA와 골드 번호 구슬로만 표현하는 신뢰 가능한 한국형 행운 서비스.

### 사용자가 느껴야 할 감정

- 첫인상: 간단하다, 바로 검색할 수 있다.
- 사용 중: 재미있지만 과장하거나 현혹하지 않는다.
- 결과 확인: 내 꿈에서 나온 숫자가 일반 무작위 숫자와 분명히 구분된다.
- 재방문: 무제한으로 눌러보는 장난감이 아니라 오늘 확인하고 기록할 가치가 있다.

### 시그니처 한 가지

실제 꿈에서 유래한 숫자만 “골드 번호 구슬”로 일관되게 표현한다.

- 골드 원형 장식을 아무 곳에나 뿌리지 않는다.
- 실제 숫자가 있을 때만 사용한다.
- 색만으로 의미를 전달하지 말고 `꿈 숫자` 라벨·범례·접근 가능한 텍스트를 함께 제공한다.
- 일반 보완 숫자는 따뜻한 흰색/아이보리 표면과 딥레드 테두리 또는 중립 테두리로 구분한다.

---

## 3. 디자인 시스템을 먼저 고정

구현 전에 현재 문서 구조를 확인하고 다음을 새로 만들거나 갱신한다.

- `docs/DESIGN_DIRECTION.md`
- `docs/DESIGN_SYSTEM.md`

프로젝트에 동일 목적의 문서가 있으면 중복 파일을 만들지 말고 기존 문서를 갱신한다.

### 3.1 컬러 토큰

아래 값은 디자인 의도와 기본 기준이다. 현재 Tailwind/CSS 변수 체계에 맞춰 semantic token으로 구현하고 컴포넌트 곳곳에 임의 hex를 반복하지 않는다.

| 역할 | 기준값 | 용도 |
|---|---:|---|
| Background | `#FFFDFC` | 전체 배경 |
| Surface | `#FFFFFF` | 폼·필요한 표면 |
| Text | `#18181B` | 제목·본문 |
| Muted text | `#68615C` | 보조 설명 |
| Border | `#E8E2DC` | 구분선·입력 테두리 |
| Primary red | `#B42318` | Primary CTA·현재 탭·핵심 링크 |
| Primary hover | `#8F1D14` | hover/pressed |
| Soft red | `#FFF0ED` | 매우 제한적인 강조 표면 |
| Gold | `#F4B740` | 실제 꿈 숫자·행운 결과 강조 |
| Soft gold | `#FFF6D8` | 로그인 혜택 또는 숫자 보조 표면 |
| Success | 기존 semantic token 우선 | 성공 상태 |
| Error | 기존 semantic token 우선 | 오류 상태 |

대략적인 화면 비율:

- 흰색/아이보리 75~80%
- 검정에 가까운 텍스트·중립색 12~15%
- 딥레드 5~8%
- 골드 3~5%

### 3.2 색상 사용 규칙

- Primary CTA는 딥레드 바탕 + 흰 글자다.
- 골드는 CTA 배경이 아니라 실제 행운 숫자와 제한적인 결과 강조에 쓴다.
- 홈 전체를 빨갛게 만들거나 순수한 원색 `#FF0000`/`#FFFF00`를 넓게 사용하지 않는다.
- 그라데이션, glow, glassmorphism, 네온, 번쩍임, 카지노 조명, 룰렛·슬롯머신 인상을 금지한다.
- 기존 브랜드 파란색은 헤더, 버튼, 링크, focus, BottomNav active, feature icon 등 브랜드 UI에서 제거한다.
- 단, 오류 없이 동작하는 제3자 UI나 실제 의미가 있는 semantic color를 무차별 문자열 치환하지 않는다.
- 꿈 숫자와 무작위 숫자의 구분을 기존 “금색/파란색”에만 의존했다면 파란색을 제거하고 라벨 + 형태/테두리 + 색을 함께 사용한다.
- 모든 텍스트와 컨트롤은 WCAG AA 대비를 충족한다.

### 3.3 타이포그래피

- 현재 한글 UI 폰트가 적절하면 유지한다. 새 외부 폰트를 설치하지 않는다.
- H1:
  - 모바일 약 32~36px, line-height 1.2~1.3
  - 데스크톱 약 48~56px, line-height 1.15~1.22
  - 과도한 letter-spacing이나 영문 랜딩페이지 같은 초대형 크기를 사용하지 않는다.
- 본문:
  - 모바일 16px 이상
  - 읽기 긴 본문 line-height 1.65~1.8
- 버튼/label은 14~16px 범위에서 명확하게 한다.
- 굵기는 3단계 정도로 제한하고 모든 문구를 굵게 만들지 않는다.

### 3.4 Shape·border·shadow

- radius는 소수의 단계만 사용한다. 예: 8 / 12 / 16px.
- 모든 내용을 둥근 카드 안에 넣지 않는다.
- Home 주요 기능은 카드 3개가 아니라 구분선이 있는 열린 레이아웃으로 만든다.
- shadow는 입력 focus나 떠 있는 모바일 내비게이션 등 목적이 있는 곳만 매우 약하게 쓴다.
- pill badge, 의미 없는 캡슐, 장식용 숫자 `01/02/03`을 사용하지 않는다.

### 3.5 Icon·motion

- 기존의 일관된 아이콘 세트가 있다면 그것만 사용한다.
- emoji를 UI 아이콘으로 사용하지 않는다.
- 홈 Hero에는 등장 애니메이션을 넣지 않는다.
- 번호 생성 결과의 유일한 주도 모션은 번호 구슬의 짧은 순차 reveal이다.
  - 전체 700ms 안팎
  - opacity + 작은 translate/scale 정도
  - 슬롯 회전, 과도한 bounce, confetti 금지
  - `prefers-reduced-motion`에서는 즉시 표시

---

## 4. Home 전체 정보 구조

Home은 아래 네 구역만 남긴다.

1. Header
2. 꿈 검색 Hero
3. 핵심 기능 3개
4. 로그인 혜택 + 신뢰 문구 + Footer

현재 Home에 다음이 있으면 제거하거나 Home 노출만 중단한다.

- 일반적인 브랜드 슬로건과 중복 설명
- Hero의 추천 검색어 chip 6개
- 동일 위계의 큰 CTA 2개
- 번호 생성·꿈해몽·운세·다이어리·당첨확인을 동일한 카드 5개로 나열한 영역
- 데이터가 비어 있는 `이번 주 인기`
- 아직 제공하지 않는 `1년치 연말 결산 리포트` 등 미래 기능 약속
- 근거 없는 `AI 추천 번호`, 당첨 확률 향상 암시
- 같은 링크나 설명의 반복

기능과 route 자체를 삭제하지 않는다. 홈에서 우선순위만 낮추며 BottomNav와 직접 URL 접근은 보존한다.

---

## 5. Home Hero — 문구와 구조 확정

Hero에는 아래 요소만 둔다. 부제, badge, 추천 chip, 장식 일러스트, 큰 보조 버튼을 새로 추가하지 않는다.

### H1

> 어젯밤 꿈, 무슨 뜻일까요?

### 검색 폼

- placeholder: `꿈에서 무엇이 나왔나요?`
- 제출 버튼: `해몽 보기`

시각 구조:

> [ 꿈에서 무엇이 나왔나요?             ][ 해몽 보기 ]

### Secondary text link

> 꿈 없이 바로 번호 만들기 →

### 5.1 레이아웃

- Hero는 중앙 정렬할 수 있지만 전형적인 badge + 긴 부제 + CTA 2개 구조는 만들지 않는다.
- max-width는 약 720~760px로 제한한다.
- 데스크톱에서는 충분한 상하 여백으로 검색 행동에 집중시킨다.
- 390px에서도 검색 input과 버튼을 한 줄로 유지한다.
  - input은 `min-width: 0`/`flex: 1`
  - 버튼은 읽을 수 있는 최소 폭
  - 전체 높이는 약 52~56px
  - 가로 overflow가 없어야 한다.
- 버튼과 input을 하나의 검색 control처럼 묶되 focus와 오류 상태가 명확해야 한다.
- Secondary link는 Primary button과 경쟁하지 않는 text link로 둔다.

### 5.2 검색 동작

- 현재 동작하는 꿈 검색·autocomplete·검색 route를 조사해 그대로 재사용한다.
- 검색어를 입력하고 Enter 또는 `해몽 보기`를 누르면 동일하게 동작한다.
- 빈 값 제출 시 페이지 이동이나 analytics 이벤트를 만들지 않는다.
- 입력창에 focus를 돌리고 `꿈에 나온 대상이나 상황을 입력해 주세요.` 같은 짧은 오류를 `aria-live`로 알린다.
- autocomplete가 있다면 키보드 ↑/↓, Enter, Escape, focus, screen reader semantics를 보존·개선한다.
- Home 검색 제출에는 기존 `dream_search_submitted` 이벤트를 재사용한다. 새 analytics provider를 만들지 않는다.
- 검색어 원문을 analytics에 보내는 현재 정책이 개인정보/검색어 정책과 맞는지 확인하고 기존 정책을 보존한다.

### 5.3 Secondary link 동작

- `꿈 없이 바로 번호 만들기 →`는 일반 `/generate` 진입으로 연결한다.
- 이 링크로 번호 생성 페이지에 들어갔다는 이유만으로 번호를 자동 생성하거나 일일 횟수를 차감하지 않는다.
- 기존 analytics abstraction으로 `source=home_secondary` 정도의 비식별 source를 기록할 수 있다.

---

## 6. Home 핵심 기능 3개

Hero 다음에는 `행운을 확인하는 세 가지 방법`이라는 짧은 섹션 하나만 둔다.

둥근 카드 3개와 큰 그림자를 사용하지 않는다.

- 데스크톱: 3열의 열린 레이아웃 + 얇은 세로 구분선
- 모바일: 한 열 + 얇은 가로 구분선
- 각 항목은 작은 기존 아이콘, 제목, 한 문장, text link만 가진다.
- 전체 섹션 배경은 기본 배경을 유지한다.

문구:

### 꿈해몽

> 꿈에 나온 장면의 의미와 그 꿈에서 얻은 행운 숫자를 확인해보세요.

링크:

> 꿈해몽 둘러보기

### 오늘의 행운

> 생년월일과 선택 정보를 바탕으로 오늘의 흐름을 확인해보세요.

링크:

> 오늘의 행운 보기

### 오늘의 세 조합

> 무제한 생성 대신, 하루 세 조합만 만들고 천천히 골라보세요.

링크:

> 오늘의 번호 만들기

실제 구현과 다른 내용을 쓰지 않는다. 오늘의 행운이 아직 비회원에게 열려 있지 않거나 일일 정책이 구현되지 않은 상태라면 기존 코드를 먼저 확인하고, 거짓 문구를 노출하지 말고 최종 보고에 blocker로 명시한다.

---

## 7. 로그인 혜택 영역

핵심 기능 아래에 하나의 간결한 benefit band를 둔다. 카드 여러 개로 쪼개지 않는다.

### 비회원

제목:

> 로그인하면, 행운이 기록으로 이어져요

본문:

> 카카오로 로그인하면 오늘의 행운 정보를 매번 입력하지 않고, 만든 번호와 행운일기를 저장해 토요일 결과와 비교할 수 있어요.

CTA:

> 카카오로 계속하기

- 현재 구현된 안전한 `next/reason` 체계를 재사용한다.
- 현재 enum에 없는 reason을 임의 문자열로 만들지 않는다.
- 로그인 후 Home 또는 사용자가 이어갈 적절한 기존 화면으로 안전하게 돌아오게 한다.
- Kakao가 제공하지 않는 정보까지 자동으로 저장한다고 암시하지 않는다.

### 로그인 회원

- 로그인 권유 문구를 반복하지 않는다.
- 같은 위치에서 `내 기록 이어보기`와 가장 적절한 기존 route를 제공한다.
- 별도의 새 대시보드를 만들지 않는다.

### 신뢰 문구

Benefit band 아래 또는 Footer 직전에 다음 취지의 짧은 문구를 둔다.

> 운세와 번호는 재미와 참고용이며, 당첨을 보장하거나 확률을 높이지 않습니다. 복권은 만 19세 이상만 구매할 수 있습니다.

- 운세 열람 자체를 만 19세 이상으로 제한하지 않는다.
- 복권 구매 연령과 운세 열람 정책을 혼동하지 않는다.

---

## 8. Header·BottomNav·공통 UI

- 기존 정보 구조와 5탭 BottomNav는 보존한다.
- Home을 단순하게 만들기 위해 route나 BottomNav 탭을 다시 삭제하지 않는다.
- active 상태, 주요 버튼, 핵심 링크의 파란색을 Primary red로 변경한다.
- BottomNav는 390px에서 label 잘림, icon/label 겹침, safe-area 문제 없이 유지한다.
- Header 로고는 현재 브랜드명을 유지하고, 불필요한 장식 또는 큰 슬로건을 추가하지 않는다.
- 모든 터치 target은 가능한 한 44×44px 이상이다.
- focus-visible은 딥레드 계열의 명확한 ring과 offset을 사용한다.
- hover만으로 의미를 전달하지 않는다.

---

## 9. “오늘의 세 조합” 제품 정책

기존 “무제한 다시 생성”을 다음 정책으로 바꾼다.

> 사용자는 한국 날짜 기준 하루에 최대 세 조합을 새로 만들 수 있다. 같은 날 다시 방문하면 이미 만든 세 조합을 다시 볼 수 있다.

“3회 클릭”보다 사용자에게 의미 있는 `오늘의 세 조합`으로 표현한다.

### 9.1 공통 규칙

- 한 번 생성할 때 1~45 사이 중복 없는 숫자 6개로 구성된 조합 한 개가 생긴다.
- 새 조합은 이전 조합을 덮어쓰지 않고 아래에 누적한다.
- 최대 3개까지 만든다.
- 현재 상태를 `오늘 0/3`, `오늘 1/3`처럼 보여준다.
- 세 조합을 만든 뒤에는 새 생성 CTA를 제거하거나 비활성화하고 다음을 보여준다.

> 오늘의 세 조합을 모두 만들었어요. 내일 0시에 다시 만들 수 있어요.

- 기준 날짜는 브라우저 실행 후 24시간이 아니라 `Asia/Seoul` 달력 날짜다.
- 생성한 조합을 삭제하거나 저장 취소해도 그날의 생성 가능 횟수는 복구되지 않는다.
- 생성 결과를 다시 열람하는 것은 횟수를 차감하지 않는다.
- 숫자 정렬 정책은 기존 정책을 확인해 일관되게 유지한다.
- `AI 추천`, `당첨 확률 상승` 같은 표현을 사용하지 않는다.

### 9.2 직접 진입

`/generate` 또는 BottomNav의 번호생성 탭으로 직접 들어오면 자동 생성하지 않는다.

초기 문구:

H1:

> 오늘의 세 조합

설명:

> 하루에 세 조합만 만들 수 있어요. 버튼을 누를 때마다 새로운 조합이 하나씩 기록됩니다.

CTA 상태:

- 0개: `첫 조합 만들기`
- 1개: `두 번째 조합 만들기 · 2개 남음`
- 2개: `마지막 조합 만들기 · 1개 남음`
- 3개: 생성 CTA 없음, 완료 문구와 저장/결과확인 CTA

버튼을 누르기 전에는 숫자 생성, DB row 생성, quota 차감, `numbers_generated` 이벤트가 없어야 한다.

### 9.3 꿈 상세 페이지에서 진입

- 사용자가 꿈 상세 페이지의 명시적인 `이 꿈의 숫자로 번호 완성하기` CTA를 눌렀다면 이는 생성 의사 표현이다.
- 이 명시적 경로에서는 현재처럼 generator 진입 후 자동 reveal을 시작할 수 있고 조합 한 개를 정상적으로 차감한다.
- 꿈 숫자는 고정하고 나머지만 기존 로직으로 채운다.
- 이미 오늘 세 조합을 모두 만들었다면 새 조합을 만들지 않고 기존 세 조합과 limit 안내를 보여준다.
- 단순한 내부 링크나 page prefetch 때문에 자동 생성되지 않게 한다.
- 일반 source와 dream source를 데이터와 analytics에서 구분한다.

### 9.4 생성 화면 구조

- 3개의 결과를 큰 카드 3개로 만들지 않는다.
- `첫 번째 조합`, `두 번째 조합`, `세 번째 조합`을 구분선이 있는 결과 행으로 쌓는다.
- 모바일 번호 구슬은 6개가 가로폭 안에서 잘리지 않게 약 40px와 유연한 gap을 사용한다.
- 데스크톱은 필요하면 44~48px까지 키운다.
- 꿈 숫자는 골드 fill + `꿈 숫자` 라벨/범례를 사용한다.
- 나머지 숫자는 따뜻한 surface + 명확한 border를 사용한다.
- 색만으로 둘을 구분하지 않는다.
- reveal 이후 결과와 남은 횟수를 `aria-live=polite`로 알린다.
- 사용자의 focus가 예기치 않게 이동하지 않게 한다.

### 9.5 회원

- UI에서만 막지 말고 server/database 레벨에서도 하루 3개를 강제한다.
- 현재 DB 구조를 먼저 확인한다.
- 기존 저장 번호 테이블과 “일일 생성 기록”의 의미가 다르면 의도하지 않은 자동 저장을 피하기 위해 별도 구조 또는 명확한 record type을 사용한다.
- 필요한 경우 `user_id + generation_date(KST) + slot_index(1~3)`에 unique constraint 또는 동등한 강제 장치를 둔다.
- 동시에 여러 탭에서 요청해도 4번째 row가 생기지 않도록 transaction/idempotent 처리한다.
- RLS를 적용하고 사용자는 자신의 기록만 읽고 쓸 수 있어야 한다.
- 같은 날 다른 기기에서 로그인해도 동일한 세 조합을 볼 수 있어야 한다.
- 조합을 실제 “저장 번호”로 보관하는 행동은 기존 저장 CTA와 기존 저장 모델을 재사용한다. 생성됐다는 이유만으로 사용자의 저장 목록을 오염시키지 않는다.

### 9.6 비회원

- 비회원은 localStorage 기반 soft limit을 사용한다.
- 이는 “한 사람/한 컴퓨터에서 절대 3개”가 아니라 “동일 브라우저 프로필에서 한국 날짜 기준 3개”임을 기술 문서에 정확히 기록한다.
- IP 제한, device fingerprinting, 광고 식별자를 추가하지 않는다.
- 예시 저장 구조:
  - schema version
  - KST date key
  - 생성된 조합 배열
  - 각 조합의 source, 꿈 숫자 여부, 생성 시각
- 숫자 조합에는 민감정보가 없지만 schema version, 손상 데이터 복구, 크기 제한을 둔다.
- localStorage가 차단되거나 quota 오류가 나도 화면이 깨지지 않게 in-memory fallback과 짧은 안내를 제공한다.
- storage 삭제, 시크릿 모드, 다른 브라우저로 우회 가능한 한계를 막기 위한 침습적 추적을 하지 않는다.
- 같은 날 재방문하면 저장된 조합과 0~3 남은 횟수를 복원한다.
- 다음 KST 날짜가 되면 새 세 조합을 만들 수 있다. 오래된 guest 기록은 제한된 기간 뒤 정리한다.

### 9.7 비회원 → 로그인 전환

- 로그인 시 같은 KST 날짜의 guest 조합이 있다면 잃어버리지 않게 현재 인증 콜백/클라이언트 구조에 맞는 안전한 reconciliation을 구현한다.
- 회원의 기존 일일 생성 기록을 우선 보존한다.
- guest 조합은 중복을 제거한 뒤 남은 slot 범위에서만 일일 생성 기록으로 옮긴다.
- 최대 3개를 넘기지 않는다.
- 이를 사용자가 명시적으로 저장한 “저장 번호”로 자동 간주하지 않는다.
- 구현 복잡도나 기존 auth 구조 때문에 안전하게 병합할 수 없다면 조용히 잘못 병합하지 말고 guest 조합을 브라우저에서 계속 보여주며 최종 보고에 제한을 명시한다.

---

## 10. 생성 알고리즘·신뢰 문구

- 기존 꿈 숫자 연결 로직과 검증된 번호 생성 함수를 재사용한다.
- 기존 생성 함수에 편향·중복 가능성이 있는지 테스트로 확인한다.
- 일반 조합은 1~45 범위, 6개, 중복 없음 조건을 항상 만족한다.
- “AI”, “예측”, “당첨 추천”, “확률을 높인다”는 표현을 제거한다.
- 번호 페이지에는 다음 취지의 고지를 유지한다.

> 번호는 재미를 위한 조합이며 당첨을 보장하거나 확률을 높이지 않습니다.

- 복권 구매 관련 위치에는 만 19세 이상 구매 가능 고지를 유지한다.

---

## 11. 기존 페이지에 적용할 디자인 범위

### 이번 작업에서 직접 재구성

- Home
- 전역 color/typography/spacing token
- Header와 BottomNav의 브랜드 색 및 상태
- 공통 Button, Link, Input, focus state
- 번호 생성 페이지
- 꿈 숫자/일반 숫자의 공통 시각 언어

### 토큰 상속과 회귀 확인만 수행

- 꿈해몽 메인/검색/Parent/Situation
- 오늘의 행운
- 행운일기
- 당첨확인
- 로그인
- 서비스 소개
- 이용약관
- 개인정보처리방침
- demo route가 있다면 demo

이 페이지들을 이번 작업에서 전면 재배치하지 않는다. 전역 토큰 변경으로 글자, 배경, 버튼, 링크, 경계가 깨지는 부분만 수정한다.

---

## 12. SEO·시맨틱

- Home의 H1은 `어젯밤 꿈, 무슨 뜻일까요?` 한 개다.
- 기능 섹션은 H2와 의미 있는 heading hierarchy를 사용한다.
- 텍스트를 canvas나 이미지 안에만 넣지 않는다.
- 기존 꿈해몽 URL과 내부 링크를 보존한다.
- Home metadata가 현재 구조와 맞는지 확인한다.
  - title 예시: `꿈해몽과 오늘의 행운, 행운 번호 | Luck Platform`
  - description은 꿈해몽, 오늘의 행운, 번호 생성 기능을 사실대로 설명하고 당첨을 암시하지 않는다.
- canonical은 hard-coded Vercel URL을 페이지마다 흩뿌리지 말고 현재 site URL 설정을 재사용한다.
- 현재 임시 도메인을 사용하더라도 향후 `NEXT_PUBLIC_SITE_URL` 등 한 곳에서 공식 도메인으로 전환 가능해야 한다.
- robots/sitemap에 공개 핵심 route가 유지되는지 확인한다.
- demo/빈 FAQ 등 이번 범위 밖의 기존 SEO 문제는 임의로 대규모 수정하지 말고 최종 보고에 분리한다.

---

## 13. Analytics

현재 analytics provider와 event naming convention을 먼저 확인하고 중복 계층을 만들지 않는다.

최소한 다음 행동을 구분한다.

| 행동 | 기존 이벤트 재사용 또는 권장 이벤트 |
|---|---|
| Home 꿈 검색 성공 제출 | `dream_search_submitted` |
| Home 번호 링크 클릭 | 기존 CTA 이벤트 또는 `home_generate_link_clicked` |
| 번호 페이지 열람 | `generate_page_viewed` |
| 실제 생성 요청 | `numbers_generation_started` |
| 6개 번호 reveal 완료 | `numbers_generated` |
| 일일 3개 상태 도달/4번째 시도 차단 | `numbers_limit_reached` |
| 저장 CTA 클릭 | 기존 저장 이벤트 |
| 실제 저장 완료 | 기존 `number_saved` |
| 로그인 혜택 CTA 클릭 | 기존 `login_started`에 source/reason |

규칙:

- page mount를 번호 생성으로 집계하지 않는다.
- 같은 저장 결과를 다시 보는 행동을 생성으로 집계하지 않는다.
- analytics에 번호 6개 전체, 꿈 검색 원문, 생년월일 등 불필요한 raw 값을 넣지 않는다.
- `source`, `auth_state`, `slot_index`, `has_dream_numbers` 같은 비식별 속성만 사용한다.
- 이미 정의된 이벤트가 있다면 이름을 추가로 만들지 말고 기존 것을 연결한다.

---

## 14. 반응형·접근성·성능

다음 폭에서 실제 렌더링을 확인한다.

- 390px
- 768px
- 1440px
- 1920px

필수 기준:

- 가로 스크롤 없음
- Hero 검색창·버튼 잘림 없음
- 한글 H1의 어색한 한 글자 줄바꿈 없음
- BottomNav safe-area와 label 정상
- 번호 구슬 6개 잘림 없음
- 긴 꿈해몽 본문 line length와 링크 대비가 전역 토큰 변경 후 깨지지 않음
- 키보드만으로 검색, autocomplete, CTA, 번호 생성, 저장 CTA 접근 가능
- focus-visible 명확
- form label 또는 접근 가능한 name 존재
- 오류·남은 횟수·생성 완료를 screen reader가 인식
- `prefers-reduced-motion` 대체
- 레이아웃 이동을 유발하는 늦은 폰트/이미지 추가 금지
- 새 무거운 이미지나 animation dependency 금지
- 목표: LCP 2.5초 이내, INP 200ms 이내, CLS 0.1 이하

---

## 15. 테스트

기존 테스트를 삭제하거나 assertion을 약화해 통과시키지 않는다.

### 15.1 Home

- H1이 정확히 한 개
- Hero에 지정 문구, 검색 폼, Secondary text link만 존재
- 기존 추천 chip 6개와 큰 CTA 2개 구조가 제거됨
- 빈 검색 제출 차단
- Enter와 버튼 제출이 동일하게 동작
- autocomplete 키보드 동작 회귀 없음
- 꿈 검색 route/URL 회귀 없음
- guest/member benefit band 상태
- 로그인 `next/reason` 안전성 회귀 없음

### 15.2 디자인 시스템

- semantic token 사용
- brand UI에 예전 파란색이 의도치 않게 남지 않음
- 꿈 숫자와 일반 숫자가 색 없이도 label/구조로 구분됨
- 대비와 focus state
- 전역 토큰 변경으로 약관·개인정보·꿈 본문 가독성이 깨지지 않음

### 15.3 일일 세 조합

- 직접 `/generate` 진입 시 자동 생성 없음
- 첫/두 번째/세 번째 생성이 각각 한 조합만 추가
- 4번째 생성 차단
- 같은 KST 날짜 재방문 시 동일 조합 복원
- 다음 KST 날짜에 3개 새로 생성 가능
- 날짜 경계 23:59 → 00:00
- 삭제/저장 취소가 quota를 복구하지 않음
- guest localStorage 정상·손상·구버전·차단 상태
- 회원 DB unique/RLS/idempotency
- 동시 요청으로 4개가 생기지 않음
- guest → member 병합 시 중복·3개 초과 없음
- dream source는 꿈 숫자 고정
- dream source 자동 생성은 명시적 CTA에서만 발생
- limit 상태의 dream source는 새 생성 없음
- 모든 조합이 1~45, 6개, 중복 없음
- 생성 전 DB row/`numbers_generated` 없음
- 실제 reveal 완료 뒤에만 `numbers_generated`

### 15.4 품질 명령

저장소의 실제 script 이름을 확인해 가능한 검증을 모두 실행한다.

- formatter/check
- lint
- typecheck
- unit/integration tests
- production build
- Playwright 또는 기존 browser smoke test
- 주요 route console error 및 broken link
- metadata/robots/sitemap smoke check

---

## 16. 스크린샷 검수

최소 다음 상태를 390px과 1440px에서 캡처한다. 가능하면 768px과 1920px도 확인한다.

1. Home Hero
2. Home 전체
3. 번호 생성 초기 0/3
4. 번호 생성 1/3
5. 번호 생성 완료 3/3
6. 꿈 숫자가 포함된 조합
7. 로그인/비로그인 benefit band

캡처를 직접 비교해 다음을 확인한 뒤 필요한 수정까지 완료한다.

- 첫 시선이 H1 → 검색 input → `해몽 보기` 순서인가
- `꿈 없이 바로 번호 만들기 →`가 Primary와 경쟁하지 않는가
- 딥레드와 골드가 화면을 지배하지 않고 핵심 행동과 실제 숫자에만 쓰였는가
- 카드·pill·shadow가 다시 과잉 사용되지 않았는가
- 390px에서 검색창과 번호 6개가 자연스러운가
- 홈이 기존보다 짧고 직관적인가
- 카지노/사행성 추천 사이트처럼 보이지 않는가

---

## 17. 완료 조건

다음 조건이 모두 충족되어야 완료다.

- Home이 지정된 4구역으로 단순화됨
- Hero가 사용자가 확정한 정확한 구조와 문구를 따름
- Primary Action은 꿈 검색 하나뿐임
- 흰색/딥레드/골드 디자인 시스템이 전역 token으로 구현됨
- 파란색 브랜드 UI가 새 체계로 정리됨
- 기존 기능·데이터·인증·SEO URL이 보존됨
- 직접 번호 탭 진입은 자동 생성하지 않음
- 번호는 하루 최대 세 조합이며 같은 날 재확인 가능
- 회원은 서버에서도, 비회원은 브라우저 soft limit으로 정책이 동작
- 꿈 숫자 연동과 시각 구분이 보존됨
- 과장·당첨 보장·AI 추천 문구가 없음
- 390/768/1440/1920px 회귀 검수
- lint/typecheck/test/build 통과 또는 기존 실패와 신규 실패가 명확히 분리됨

---

## 18. 최종 보고 형식

다음 형식으로 짧고 구체적으로 보고한다.

### A. 사전 감사

- 작업 전 dirty state
- 보존한 기존 변경
- 기준 build/lint/test 상태

### B. 디자인 결과

- 확정한 design thesis
- token 변경
- Home에서 제거/유지/추가한 요소
- 파란색 → 딥레드/골드 매핑

### C. 제품 정책 결과

- “하루 세 조합”이 정확히 무엇을 제한하는지
- 회원/비회원 저장과 제한 방식
- KST reset
- dream source 동작
- guest soft limit의 우회 가능성

### D. 변경 파일

- 파일별 핵심 변경
- DB migration/RLS가 있다면 정확한 내용
- analytics 연결

### E. 검증

- 실행한 명령
- 테스트 통과 수
- build 결과
- 화면 폭별 캡처 경로
- 키보드/reduced-motion/contrast 확인

### F. 남은 위험과 사용자 작업

- 수동 확인이 필요한 것
- 배포 후 확인할 route
- 환경변수 또는 migration 적용 순서
- 이번 범위 밖으로 남긴 항목

마지막에 권장 commit message 하나를 제시하되, 실제 commit/push는 하지 말고 승인을 기다려라.
